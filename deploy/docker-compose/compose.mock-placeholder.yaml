# Compatibility filename for the campaign mock. The base Compose file now
# always uses the immutable encrypted-corpus source; this overlay deliberately
# adds no runtime encryption or mutable source behavior.
services: {}
