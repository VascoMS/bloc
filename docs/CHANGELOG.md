# Changelog

Use this file for implementation-level changes that do not need a full decision record.

## Entry Format

```md
- YYYY-MM-DD | milestone | area/module | change made | reason | files touched | validation performed | follow-up work
```

## Entries

- 2026-07-13 | `M4` | BTE attribution temporary SSH key | fixed Windows serialization of multiline AWS key material by restoring LF-delimited UTF-8 without BOM and added local `ssh-keygen` validation before Terraform apply | prevent PowerShell array output from concatenating PEM lines and creating unreachable benchmark instances | `deploy/ec2/run-bte-attribution.ps1`, ignored `results/ec2/bloc-ec2-bteattr-20260713T144554Z/` | multiline key round-trip with the runner's restrictive ACL passed; PowerShell parser and targeted whitespace checks passed; Terraform destroyed all 16 resources from the failed attempt and AWS follow-up found no non-terminated instances or retained local PEM | retry the unchanged `t3.small`/`c7i-flex.large` campaign matrix |
- 2026-07-13 | `M4` | BTE attribution Windows runner | fixed the inventory and credit-metric loops to avoid PowerShell's reserved, read-only `$Host` automatic variable after the first eligible four-host apply stopped before measurements | allow the committed Windows runner to iterate over provisioned hosts without a runtime variable collision | `deploy/ec2/run-bte-attribution.ps1`, ignored `results/ec2/bloc-ec2-bteattr-20260713T143737Z/` | PowerShell parser, reserved-variable scan, and targeted whitespace checks passed; Terraform destroyed all 16 resources; AWS follow-up found all four instances terminated, empty Terraform state, no retained local PEM, and complete cleanup evidence | retry the unchanged `t3.small`/`c7i-flex.large` campaign matrix |
- 2026-07-13 | `M4` | BTE attribution campaign configuration | parameterized the second two-placement instance class and recorded the approved `c7i-flex.large` Free Tier-compatible revision while retaining `c7a.large` as the default reference design | make the account-constrained Intel substitution explicit in manifests and reports instead of silently changing the intended AMD comparison | `deploy/ec2/run-bte-attribution.ps1`, `docs/STATUS.md`, `docs/WORKFLOWS.md` | PowerShell parser and targeted whitespace checks passed; AWS reports `c7i-flex.large` offered in both `us-east-1a` and `us-east-1b` | rerun with `-ComparisonInstanceType c7i-flex.large`, validate all four host artifacts, and report the hardware distinction |
- 2026-07-13 | `M4` | BTE attribution EC2 preflight | attempted the committed four-host campaign, recorded the account's Free Tier-only rejection of both `c7a.large` placements, verified that Terraform destroyed all partially created AWS resources, and fixed cleanup so empty state evidence is materialized and the local temporary PEM is deleted | preserve the controlled hardware design instead of silently substituting an instance type, while hardening cleanup discovered through the failed apply | `deploy/ec2/run-bte-attribution.ps1`, `docs/STATUS.md`, ignored `results/ec2/bloc-ec2-bteattr-20260713T142745Z/` | AWS follow-up found both temporary `t3.small` instances terminated and zero volumes, VPCs, ECR repositories, or AWS key pairs; PowerShell parser and targeted whitespace checks passed; the retained local PEM from the failed attempt was removed | either remove the account restriction and retain `c7a.large`, or explicitly approve and document `c7i-flex.large` as the comparison host before retrying |
- 2026-07-13 | `M4` | BTE combine attribution, EC2 automation | added a randomized paper-to-BLOC combine attribution ladder, exact variant filtering and focused CPU profiles, a placement-aware report that imports existing sidecar context, a dedicated benchmark image, and a four-host `t3.small`/`c7a.large` Terraform campaign runner with CPU-credit capture and strict artifact acceptance | isolate whether the reported combine-latency gap comes from hardware, planning/index shape, threshold, proof verification, or the hybrid wrapper while keeping network/ACS work outside the controlled comparison | `bte/btd-impl-main/cmd/bte-attribution/`, `bte/btd-impl-main/README.md`, `bte/btd-impl-main/TESTING.md`, `deploy/ec2/bte-attribution.Dockerfile`, `deploy/ec2/bte-attribution/terraform/`, `deploy/ec2/run-bte-attribution.ps1`, `docs/STATUS.md`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md` | `go test ./...` passed in the BTE module; timed-matrix, exact-filter CPU-profile, and report smokes passed; PowerShell parser and `git diff --check` passed; Terraform formatting and isolated init/validate passed; the Docker image built and both the attribution executable and inherited paper benchmark passed runtime-image smokes | commit the campaign changes, review the Terraform plan, then launch the paid campaign with explicit operator approval |
- 2026-07-06 | `M3` | EC2 campaign automation, Terraform | added generated multi-AZ subnet support to the EC2 Terraform module, added a Windows M3 cross-AZ synthetic wrapper, and ran the `n=4/n=7` cross-AZ campaign cleanly | collect the first same-region cross-AZ comparison dataset while keeping the same cost controls and cleanup behavior as the same-AZ path | `deploy/ec2/terraform/main.tf`, `deploy/ec2/terraform/variables.tf`, `deploy/ec2/run-a1-pilot.ps1`, `deploy/ec2/run-m3-cross-az.ps1`, `docs/STATUS.md`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md`, `results/ec2/m3-cross-az-synthetic-20260706t122922z/`, `results/charts/m3-cross-az-synthetic-20260706t122922z/` | PowerShell parser checks passed for EC2 runner scripts; `terraform fmt -check -diff` and `terraform validate` passed; Docker and AWS STS preflights passed; M3 cross-AZ ran `n=4` and `n=7` with batches 8/32/128, 5 warmups, and 30 measured repetitions per batch; all 180 measured runs succeeded and were cross-node consistent; Prometheus saw 4/4 and 7/7 targets up; charts generated; Terraform destroy completed for both phases; cleanup verification and AWS follow-up checks found no tagged instances, volumes, VPC, ECR repository, temporary key pair, IAM role, or instance profile | compare same-AZ and cross-AZ chart outputs, then decide whether to request a vCPU quota increase for comparable `n=10` EC2 evidence |
- 2026-07-05 | `M3` | EC2 campaign automation | added the Windows M3 same-AZ synthetic campaign wrapper, resource sampling in EC2 phases, EBS volume tags, and expanded cleanup verification for volumes and IAM leftovers | promote the clean A1 pilot into a thesis-grade 4/7/10-node same-AZ synthetic metric path without weakening AWS cost guardrails | `deploy/ec2/run-m3-same-az.ps1`, `deploy/ec2/run-a1-pilot.ps1`, `deploy/ec2/terraform/main.tf`, `docs/STATUS.md`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md` | PowerShell parser checks passed for EC2 runner scripts; `git diff --check` passed with only existing CRLF warnings; `terraform fmt -check -diff` and `terraform validate` passed; `cd bloc-node && go test ./...` passed; `latency-charts\.venv\Scripts\python.exe -m pytest latency-charts` passed | run the M3 wrapper starting with the 4-node phase, inspect results, then continue to 7 and 10 nodes |
- 2026-07-05 | `M3` | docs, roadmap | removed inactive orchestrated-container guidance from the active roadmap, status, validation, workflow, architecture, and module README narrative while retaining a single out-of-scope artifact note | keep thesis metric collection focused on VM/EC2-per-sidecar campaigns and avoid presenting the old deployment rehearsal as an active deployment or validation path | `docs/ROADMAP.md`, `docs/STATUS.md`, `docs/VALIDATION.md`, `docs/WORKFLOWS.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `bloc-node/README.md` | documentation consistency scan with `rg` plus targeted `git diff --check` | revive the retained manifests only if a future milestone explicitly brings that deployment path back into scope |
- 2026-07-05 | `M3` | EC2 pilot runner, charts | changed A1 pilot network characterization from ICMP ping to controller-to-operator HTTP `/healthz` timing, ran the automated A1 same-AZ pilot cleanly, and made the chart loader accept UTF-8 BOM manifests emitted from Windows artifacts | default EC2 security groups do not allow ICMP, while BLOC traffic uses HTTP/libp2p ports; ping loss was misleading pilot evidence; generated PowerShell JSON artifacts need to remain chart-compatible | `deploy/ec2/run-a1-pilot.ps1`, `latency-charts/src/bloc_latency_charts/data.py`, `latency-charts/tests/test_charts.py`, `docs/STATUS.md`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md`, `results/ec2/bloc-ec2-a1-pilot-same-az-n4-20260705-192544/`, `results/charts/bloc-ec2-a1-pilot-same-az-n4-20260705-192544/` | PowerShell parser validation passed for both EC2 runner scripts; targeted `git diff --check` passed with only existing CRLF warnings; Docker, AWS STS, and Terraform validation preflights passed; A1 pilot ran 4 `t3.small` operators plus 1 `t3.small` controller for batches 8/32/128 with 1 warmup and 3 measured repetitions per batch; all measured runs succeeded and were cross-node consistent; Prometheus saw 4/4 targets up; HTTP timing succeeded 5/5 to each operator before and after; Terraform destroy completed and AWS follow-up checks found no tagged instances, volumes, VPC, ECR repository, temporary key pair, IAM role, or instance profile; `latency-charts\.venv\Scripts\python.exe -m pytest latency-charts` passed; charts generated from the pilot CSV output | promote the runner shape to the next controlled M3 metric increment |
- 2026-07-05 | `M3` | EC2 pilot runner | added keep-alive debugging support to the Windows A1 runner plus a rerun script that reuses a kept EC2 environment to push a fresh image, restart operator containers, and run scenarios with explicit slot offsets | avoid slow and costly Terraform create/destroy cycles while iterating on runner, image, or evaluator bugs | `deploy/ec2/run-a1-pilot.ps1`, `deploy/ec2/rerun-a1-pilot-existing.ps1`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md` | PowerShell parser validation passed for both scripts; targeted `git diff --check` passed | launch the next pilot with `-KeepResourcesOnFailure`, use `rerun-a1-pilot-existing.ps1` for image/evaluator iteration, and destroy the kept Terraform workdir promptly after a clean run |
- 2026-07-05 | `M3` | EC2 pilot runner | added a Windows PowerShell A1 pilot runner and made it the current supported path for the same-AZ 4-operator pilot while keeping the bash runner as an optional future Linux/WSL controller path | continue EC2 pilot automation from the working Windows Codex environment instead of blocking on WSL distro/tooling issues | `deploy/ec2/run-a1-pilot.ps1`, `docs/STATUS.md`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md` | PowerShell parser validation passed; `git diff --check` found no whitespace errors beyond existing CRLF warnings; `terraform fmt -check -diff deploy/ec2/terraform` passed; `terraform -chdir=deploy/ec2/terraform init -input=false` and `terraform -chdir=deploy/ec2/terraform validate` passed | run the Windows runner once local Docker, AWS CLI, and Terraform preflights pass, then inspect `results/ec2/<experiment-id>/` artifacts and cleanup verification |
- 2026-07-05 | `M3` | EC2 pilot runner | aligned ECR-backed experiment ids with the scoped `bloc-ec2-*` IAM policy, isolated each runner Terraform workdir under the result artifact directory, added an `ecr`/`ssh-load` image-distribution switch, and moved a bounded Docker server check plus image build ahead of EC2 key creation and Terraform apply | prevent IAM-prefix drift, stale Terraform variables, and local Docker failures from creating cloud resources before the pilot can run | `deploy/ec2/run-a1-pilot.sh`, `deploy/ec2/terraform/main.tf`, `deploy/ec2/terraform/variables.tf`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md` | `bash -n deploy/ec2/run-a1-pilot.sh`; runner help check; clean Terraform validation from an isolated copy; AWS cleanup verification after interrupted pilot destroy found no EC2 instances, VPC, ECR repository, IAM role, or instance profile, and the stale EC2 key pair was deleted; Docker remains unavailable from this Codex shell because the socket still returns permission denied | fix Docker Desktop or WSL Docker socket access for the shell running the pilot, confirm `timeout 20 docker version --format '{{.Server.Version}}'` works, then rerun the A1 pilot |
- 2026-07-05 | `M3` | EC2 deployment automation | added a Linux-first A1 pilot runner that provisions the 4-operator same-AZ EC2 campaign, pushes the sidecar image to ECR, deploys host-local Compose services, runs batch-size 8/32/128 `eval-remote` pilot scenarios, collects network/Prometheus/log/evaluator artifacts, merges scenario CSV/JSON outputs, optionally generates charts, and destroys resources in a cleanup trap | turn the manual EC2 smoke into a repeatable cost-controlled pilot before thesis-scale runs | `deploy/ec2/run-a1-pilot.sh`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md`, `docs/STATUS.md` | static review only in this Windows environment; WSL has no Linux distro installed, so bash execution was not available here | run the script from WSL Ubuntu or a Linux controller with AWS CLI, Terraform, Docker, `jq`, SSH, and SCP installed, then inspect the A1 pilot artifacts |
- 2026-07-05 | `M2`/`M3` | EC2 deployment, validation, docs | ran the first cost-controlled EC2-per-sidecar smoke with 4 `t3.micro` operator instances, 1 `t3.small` controller, ECR image distribution, host-local operator Compose, controller Prometheus/Grafana, and `eval-remote` from the controller | prove the VM/EC2-per-sidecar deployment path can collect protocol metrics on independent instances before longer campaigns | `deploy/ec2/*`, `docs/STATUS.md`, `.gitignore` | `terraform fmt -check -diff`; `terraform init`; `terraform validate`; `cd bloc-node && go test ./...`; Docker build/push to ECR; Terraform apply of 16 expected low-cost resources; `/healthz` and `/metrics` passed on all 4 operators; Prometheus saw 4/4 targets up; `eval-remote --batch-size 8 --warmups 0 --repetitions 1` succeeded with all node outputs consistent and `total_slot_us=90166`; artifacts collected under ignored `deploy/ec2/artifacts/ec2-smoke-20260705-1149/`; Terraform destroy removed all 16 resources and follow-up AWS checks found no running/stopped smoke instances, no smoke VPC, empty Terraform state, and no ECR repo | automate the copy/start/check sequence, then run repeated M3 EC2 campaigns with the same explicit destroy guardrail |
- 2026-07-04 | `M2`/`M3` | `bloc-node`, deployment, docs | added an EC2 inventory-to-config command plus host-local operator Compose, controller Prometheus/Grafana Compose, Terraform inventory skeleton with ECR repository and EC2 ECR-readonly instance profile support, and EC2 workflow docs | make the one-EC2-per-sidecar thesis deployment path reproducible without multi-host Compose or copied AWS credentials on operator hosts | `bloc-node/internal/app/ec2_config.go`, `deploy/ec2/*`, canonical docs | `cd bloc-node && go test ./...`; `terraform fmt -check -diff`; `terraform init`; `terraform validate`; first `terraform plan`; first apply blocked before EC2 creation by missing `ecr:CreateRepository` and `iam:CreateRole`, then partial networking resources destroyed successfully | add required ECR/IAM permissions to the deploy IAM user/group, then rerun plan/apply and collect the first `eval-remote` smoke artifacts |
- 2026-07-04 | `M2`/`M3` | docs, evaluation strategy | realigned the thesis evaluation strategy so local `eval-local`/`eval-suite` runs remain the clean protocol baseline, Docker Compose is a local deployment rehearsal, and VM/EC2-per-sidecar is the primary distributed metric-gathering environment | avoid treating one managed orchestrated-container cluster as the main DVT-style operator-independence model and reduce orchestration-specific measurement confounders | `docs/STATUS.md`, `docs/VALIDATION.md`, `docs/WORKFLOWS.md`, `docs/ROADMAP.md`, `docs/DECISIONS.md`, `docs/ARCHITECTURE.md` | documentation-only change; canonical-doc consistency review | plan the VM/EC2-per-operator deployment recipe |
- 2026-07-04 | `M2` | deployment, docs | fixed the earlier local orchestrated-container sidecar rehearsal by making pods start in parallel, adding startup-probe protection before liveness, publishing not-ready service DNS records for peer bootstrap, ignoring generated `bloc-node` cluster configs, and adding a local port-forward remote-evaluator config | allow all static libp2p peers to exist and resolve each other before readiness, while keeping trusted-dealer local config out of git | earlier deployment manifests, `.gitignore`, earlier local remote-evaluator config, `docs/STATUS.md` | `cd mempool-il && go test ./...`; `cd bloc-node && go test ./...`; Docker image build; local orchestrated-container rollout 4/4 ready; `/healthz` and `/metrics` over port-forward; `eval-remote` 1/1 success in the earlier local rehearsal result directory | primary distributed metrics move to VM/EC2-per-sidecar |
- 2026-06-30 | `M2`/`M3` | docs, workflow | clarified that local tests, local evaluator runs, and Compose runs are preflight checks only, while distributed sidecar runs are the current evidence target | keep future development focused on deployability and distributed thesis evidence instead of local timing numbers or chart generation as ends in themselves | `docs/STATUS.md`, `docs/VALIDATION.md`, `docs/WORKFLOWS.md` | documentation-only change; canonical-doc consistency review | superseded by the 2026-07-04 VM/EC2-per-sidecar evaluation strategy |
- 2026-06-28 | `M3` | `mempool-il`, `bloc-node`, deployment, docs | added corpus-backed `replay-placeholder` source mode that validates real signed Ethereum target transactions, encrypts them once into BLOC encrypted payloads, exposes mock placeholder metadata, and lets `eval-remote`/`eval-suite` select `--tx-source mock-placeholder` without changing synthetic defaults | make distributed latency evidence use realistic target transaction payloads without pretending public mempool transactions are native BLOC placeholders | `mempool-il/internal/mempool/replay_placeholder.go`, `mempool-il/internal/inclusion/builder.go`, `bloc-node/internal/app/provider.go`, evaluator tx-source options, Docker Compose mock-placeholder artifacts, canonical docs | `cd mempool-il && go test ./...`; `cd bloc-node && go test ./...`; Docker Compose mock-placeholder stack; `eval-remote` 2/2 measured successes from slots 2-3; chart generation | expand the corpus beyond the committed fixture before thesis-scale distributed runs |
- 2026-06-28 | `M3` | `mempool-il`, docs | changed mock-placeholder normalization so `encrypted_payload_hex` is parsed from signed placeholder transaction calldata instead of being treated as a parallel generated field; placeholder requested gas now drives BLOC selected-gas accounting while carrier gas remains metadata | align the mock mempool with the real intended flow where public mempool objects are placeholder transactions carrying encrypted payloads in calldata | `mempool-il/internal/mempool/classifier.go`, `mempool-il/internal/mempool/replay_placeholder.go`, docs | `cd mempool-il && go test ./...`; `cd bloc-node && go test ./...`; full mock-placeholder Compose recreate; `eval-remote` 1/1 measured success in `results/distributed/compose-mock-placeholder-calldata-clean` | expand the corpus beyond the committed fixture before thesis-scale distributed runs |
- 2026-06-28 | `M2` | `bloc-node`, deployment, Grafana, docs | replaced the hand-rendered `/metrics` text with official Prometheus collectors, seconds-based slot/HTTP latency histograms, `_total` counters, bounded labels, normalized HTTP instrumentation, and histogram-safe Grafana queries | make distributed sidecar visibility Prometheus/Grafana-native instead of a scrapeable copy of local evaluator artifacts | `bloc-node/internal/app/metrics.go`, node lifecycle metrics, Grafana dashboard JSON, canonical validation/decision docs | `cd bloc-node && go test ./...` | build/run Compose, confirm Prometheus targets and Grafana PromQL panels, then run `eval-remote` smoke |
- 2026-06-28 | `M2` | `bloc-node`, deployment, docs | added deployment-ready sidecar support with listen/advertise config fields, `NODE_ID` startup, Prometheus-compatible `/metrics`, structured slot logs, `eval-remote`, Docker image, Compose Prometheus/Grafana smoke stack, earlier orchestrated-container manifests, and deployment-first roadmap/status/validation docs | make distributed sidecar latency/performance evidence collectable before Builder API compatibility work | `bloc-node/internal/app/*`, `bloc-node/Dockerfile`, `deploy/*`, canonical docs | `cd bloc-node && go test ./...` | build/run Compose smoke, run `eval-remote`, then collect distributed sidecar charts |
- 2026-06-25 | `M1` | `sbc/hbbft`, `bloc-node`, evaluator | fixed remaining 7/10-node local liveness stalls by serializing node-local ACS driver steps, adding ACS/RBC/BBA progress snapshots, correcting BBA dual-BVAL accounting and later-epoch self-BVAL handling, adding an all-RBC ACS completion path, and making evaluator result polling report true missing node IDs instead of ready prefixes | make the local controlled M1 campaign deterministic enough to produce professor-facing baseline evidence without masking failed runs | HBBFT ACS/BBA, `bloc-node` node status/evaluator polling, focused tests | `bloc-node go test ./...`; `sbc/hbbft go test ./...`; 27/27 reduced 4/7/10 matrix; 30/30 targeted 7/10 stress matrix | rerun the complete 315-sample `m1-baseline` and regenerate charts |
- 2026-06-25 | `M1`/`M2` | docs, BTE | documented that integrated `bloc-node` and M1 runs already use BEAT-MEV `Opt-2` deterministic sub-batching, while normal/`sqrt(B)`/parallel/512-batch comparisons remain M2 benchmark dimensions | make experiment interpretation discoverable from Markdown without inspecting BTE code | `docs/ARCHITECTURE.md`, `docs/VALIDATION.md`, `docs/WORKFLOWS.md`, `docs/STATUS.md`, `bte/btd-impl-main/README.md`, `bte/btd-impl-main/CLUSTER_BTE.md`, `bte/btd-impl-main/TESTING.md` | documentation-only change; link/ownership review | decide whether to add explicit evaluator flags for BTE optimization variants during M2
- 2026-06-24 | `M1` | `bloc-node`, `sbc/hbbft`, evaluator | added replaceable slot-scoped node state, strict slot-aware control calls, stale-envelope rejection, cascading idempotent ACS shutdown, persistent operator-count clusters, deterministic grouped scheduling, recovery restarts, and separate cluster/preparation/submission evidence | amortize local setup without contaminating latency samples or weakening failure evidence | node lifecycle, HBBFT slot teardown, persistent evaluator, tests, canonical architecture/workflow/validation docs | existing app tests passed; 4-run persistent 4-node smoke and 6-run 4/7/10 matrix both passed, including batch 128 and one combine attempt on every node | run the complete persistent 315-sample M1 campaign and regenerate charts
- 2026-06-24 | `M1` | `sbc/hbbft`, `bloc-node`, evaluator, charts | made message-queue drains atomic, prevented ACS completion before all positively decided RBC outputs exist, serialized node-local BTE combination, removed raw TCP/gob transport, standardized protobuf envelopes over direct libp2p streams, added deterministic retrying peer connections and complete stream writes, reused evaluator HTTP connections, and reduced M1 to 9 libp2p scenarios/315 runs | eliminate message loss, premature nil ACS outputs, BTE races, socket churn, and duplicate transport evidence | HBBFT consensus state, `bloc-node/internal/app/*`, evaluator/profile tests, chart tests, scripts, canonical architecture/decision/validation docs | `bloc-node go test ./...`, all BTE tests, 9 chart tests, 10/10 readiness runs, and 18/18 reduced matrix runs passed; the full 315-run campaign completed with 298 successes and 17 result timeouts | diagnose remaining 7/10-node liveness stalls, add persistent-cluster campaign execution, then collect a valid M1 dataset and figures
- 2026-06-21 | `M1` | `bloc-node` evaluator and `latency-charts` | added the 18-scenario `m1-baseline` profile, resolved campaign metadata, fixed configuration fields in run CSVs, scalable readiness timeout, and separate `results/charts/<experiment-id>` output discovery | make the complete local baseline a single reproducible command while keeping figures separate from raw results | evaluator, chart CLI/tests, module READMEs, canonical validation/status docs | Go unit tests, 8 chart tests, reduced profile integration run, and chart output-path smoke passed | execute the full 630-run profile and generate `results/charts/m1-baseline`
- 2026-06-21 | `M1` | `latency-charts` | added a Python CLI that converts evaluator run measurements into SVG/PNG p50/p95 scaling, mean critical-path breakdown, and raw-run distribution charts | make configuration-dependent latency results directly inspectable and suitable for later thesis figures | `latency-charts/*`, root navigation, `docs/VALIDATION.md`, `docs/STATUS.md` | 5 pytest cases passed; all six figures rendered and visually inspected against the reduced TCP/libp2p dataset | generate figures from the full baseline
- 2026-06-21 | `M1` | `bloc-node` evaluator | added monotonic microsecond stage timing, critical-node aggregation, seeded repeated configuration suites, configuration manifests, raw/summary CSV and JSON artifacts, stricter cross-node consistency checks, and a libp2p full-mesh readiness barrier | collect configuration-linked local latency statistics without overstating them as DVT deployment evidence | `bloc-node/internal/app/*`, `bloc-node/README.md`, `docs/VALIDATION.md`, `docs/STATUS.md` | `go test ./internal/app`; reduced 4-node batch-8 TCP/libp2p suite with 1 warmup and 2 measured repetitions passed all 6 runs | run the full 12-scenario baseline
- 2026-06-21 | `M0` | docs/repo | created canonical root documentation spine and task-routing `AGENTS.md` | reduce fragmented documentation and make Codex navigation predictable | `README.md`, `AGENTS.md`, `docs/*`, module READMEs | documentation link review | keep consolidating redundant module-local notes over time
- 2026-06-21 | `M0` | docs/repo | added live `STATUS.md` and rebuilt the roadmap around RQ-tagged phased milestones | make project state resumable and keep execution linear | `docs/STATUS.md`, `docs/ROADMAP.md`, routing docs | documentation consistency review | use `M1` to drive the next implementation and evaluation cycle
