# Changelog

Use this file for implementation-level changes that do not need a full decision
record. Entries are historical records: their follow-up field describes the next
action at the time of the entry and is not a source of current project state.
Use [STATUS.md](STATUS.md) for current blockers, evidence, and next actions.

## Entry Format

```md
- YYYY-MM-DD | milestone | area/module | change made | reason | files touched | validation performed | follow-up work at the time
```

## Entries

- 2026-08-30 | `M5` | issue #23 ACS latency diagnostics local gate | added opt-in local evaluator propagation, paired proposal-ready trace-off/on benchmarks using retained batch-8/32/128 proposal sizes, output/message equivalence checks, traced ACS safety evaluator stages, and canonical trace interpretation and validation guidance | prove that bounded ACS observation is correct and below the 2% median local-latency stop threshold before any matched VM campaign | hbbft benchmark/equivalence test, bloc-node config/evaluator CLI and tests, ACS safety runner/contract, module README/deep dives, validation/status docs, and ignored local evidence | all six median latency deltas passed at `-0.085%` to `+0.757%` with byte/allocation deltas no higher than `+0.524%`/`+0.767%`; the local smoke retained 4/4 validated traces; all normal module/chart suites and focused host races passed; clean campaign `issue-23-local-gate-87ae409` passed repeated safety tests, 100/100 sustained plus 180/180 matrix measurements, module identity checks, and retained all 1,509 expected traces with no AWS allocation; the exact race selection also passed in a network-disabled Linux/ARM64 container with read-only source/module-cache mounts and retained image/commit/source/log identities | freeze immutable diagnostic images/bundles, then request separate authorization for the smallest matched same-AZ/three-region canary |

- 2026-08-19 | `M5` | issue #16 accepted n7 three-region latency | executed and accepted `bloc-ec2-i16-tr-n7-latency-p1`, completing issue #16's explicitly amended latency-only scope | collect the p99-capable n7 WAN half of the matched primary latency matrix without starting deferred resource, extension, or economic-analysis work | ignored n7 artifact, `docs/STATUS.md`, `docs/VALIDATION.md`, this changelog, and issue #16 evidence | exact frozen preflight passed; all 42 Terraform resources applied; immutable staging, images, services, and health passed; all 30 durable jobs exited zero; every batch retained exactly 1,000/1,000 successful, consistent attempts within 12 seconds over 10 balanced blocks; Type-7 p99 was 654.747/1,384.713/3,800.734 ms with 95% order-statistic intervals 608.808--798.036/1,246.767--1,650.390/3,654.752--4,233.361 ms for batches 8/32/128; stage additivity and both final artifact validators pass; Terraform destroyed all 42 resources and deleted all three keys; retained and independent authenticated regional EC2/network/key, IAM, and Terraform-state checks are empty | close issue #16 as completed for latency only; leave issue #15 resource work paused and obtain explicit direction before selecting the next milestone task |

- 2026-08-19 | `M5` | issue #16 accepted n4 three-region latency | fixed the post-destroy cleanup JSON assembler, replayed authenticated absence checks, and accepted retained run `bloc-ec2-i16-tr-n4-latency-p1` without rerunning measurement | the complete 3,000-attempt dataset was invalidated only because Bash `${value:-{}}` appended an extra closing brace to each non-empty region record before `jq --argjson` | three-region topology adapter and lifecycle regression, frozen execution overlay, ignored n4 artifact, `docs/STATUS.md`, this changelog, and issue #16 evidence | the new full-assembler regression failed with `jq: invalid JSON text passed to --argjson` before the fix; focused and complete lifecycle/adapter tests, Bash syntax, and campaign-runner portability pass; task/frozen adapter SHA-256 is `f2628633824ecf2a624d16630f71c01e593ad5921c09a81f15b395373b6624ec`; all 3,000 attempts were successful, consistent, and within 12 seconds; Type-7 p99 was 539.382/1,039.952/3,131.810 ms for batches 8/32/128; fresh authenticated regional EC2/network/key, IAM, and Terraform-state checks are empty; final cleanup and phase validators pass, and the manifest is complete with replay events preserving the original cleanup failure | refresh the exact frozen n7 three-region contract, then request separate authorization for the n7 latency run; keep resource, extension, and economic work deferred |

- 2026-08-18 | `M5` | issue #15 rejected n4 resource p4 | retained `bloc-ec2-i15-sa-n4-resource-p4` invalid after the corrected live sampler gate rejected the first block/batch cell before evaluator measurement | preserve the 250 ms resource-evidence contract rather than accepting slow or truncated samples | ignored p4 artifact, `docs/STATUS.md`, this changelog, and issue #15 | provisioning, staging, exact images, service startup, and health passed; three operator CSVs retained 7--9 samples about 2.0 seconds apart and fail the cadence validator, while node 2 exited after two rows and fails the minimum; source inspection shows each nominal 250 ms iteration invokes bounded `docker stats --no-stream` for network counters and any transient sample failure terminates silently under `set -e`; Terraform destroyed all 15 resources and the key, cleanup validation passes, fresh instance/VPC queries are empty, the IAM role is absent, Terraform state is empty, and no local key remains | obtain an explicit decision to preserve 250 ms sampling with a non-blocking network-counter source plus failure diagnostics, or deliberately relax the evidence cadence; do not launch n7 |

- 2026-08-18 | `M5` | issue #15 final resource sampler lifecycle | replaced the phase-wide asynchronous sampler with one region-labeled segment per balanced block/batch cell, required live PID/header/four-row readiness and bounded validated shutdown, made every operator resource recovery mandatory, and made final acceptance reconstruct and validate exact segment coverage before generating per-segment and per-batch/node/cluster summaries | prevent a complete evaluator matrix or manifest flag from masking absent or malformed resource evidence, as happened in rejected n4 resource p3 | final lifecycle and artifact helpers/tests, frozen execution overlay, EC2 runbook, validation/status docs, this changelog, and issue #15 | regression tests first reproduced the missing region, absent lifecycle gates, optional recovery, and manifest-only acceptance; 33 artifact tests plus lifecycle, final contract, remote-job, race-gate, Terraform contract, runner portability, both direct Terraform validations, Bash/Python syntax, diff hygiene, task/frozen SHA equality, and exact n4 p4/n7 p1 resource `--validate-only` pass; no AWS API or billable resource was used for the correction | run n4 resource p4 from zero, accept evidence and cleanup, then run n7 resource p1 |

- 2026-08-17 | `M5` | issue #15 rejected n4 resource p3 | reclassified `bloc-ec2-i15-sa-n4-resource-p3` invalid after all evaluator work completed but every operator sampler exited immediately and no resource CSV was collected | do not accept a complete evaluator matrix or manifest sampler flag as resource evidence | ignored p3 artifact, `docs/STATUS.md`, this changelog, and issue #15 | all 3,000 evaluator attempts and 30 controller jobs completed, lifecycle initially reported every stage `ok`, Terraform destroyed 15 resources and the key, and authenticated cleanup is empty; recovered sampler logs prove the launch omitted required `--region`; asynchronous start/stop had no liveness/minimum-row gate, recovery suppressed missing resource files, and final-phase validation never invoked resource coverage/cadence/counter/restart/OOM checks | obtain explicit approval for a fail-closed per-configuration sampler lifecycle, mandatory recovery, and final resource validator/summary before rerunning n4 from zero; do not launch n7 |

- 2026-08-17 | `M5` | issue #15 remote-job exit publication | rechecked durable `exit.status` after a failed PID liveness test before classifying a reconnectable evaluator job as `LOST` | close the exact check-then-act race proven by rejected n4 resource p2 without retrying, duplicating, or changing evaluator work | remote-job helper/test, frozen execution overlay, EC2 runbook, validation/status docs, this changelog, and issue #15 | the deterministic regression published `exit.status=0` during the liveness check and first failed with `LOST`; it passes after the four-line correction, along with helper, lifecycle, contract, Terraform, runner-portability, 29 artifact, race-gate-contract, and Bash syntax suites; task/frozen helper SHA-256 is `a0bfb2e28e04bed69c2a7394b3be16440f89c35051146efa1899d20869949cac` | validate exact n4/n7 resource contracts, then run n4 p3 from zero before n7 |

- 2026-08-16 | `M5` | issue #15 rejected n4 resource p2 | retained invalid `bloc-ec2-i15-sa-n4-resource-p2` after a reconnectable-job status race falsely reported a completed block-2 batch-128 cell as `LOST`, then completed authenticated cleanup | preserve fail-closed evidence and stop before silently changing frozen supervision tooling or launching n7 | ignored p2 artifact, `docs/STATUS.md`, this changelog, and issue #15 | p2 passed provisioning, staging, exact images, all four service starts, health, and sampler start; the supervisor rejected the run at block 2/batch 128, while recovered durable state proves `exit.status=0`, 100/100 successful attempts at slots 401--500, and empty stderr; the initial exit-file test can race with status publication before the dead-PID test, which does not recheck the file; sampler stop/recovery passed, Terraform destroyed 15 resources and deleted the key, and cleanup has successful empty EC2/IAM queries plus empty state | obtain an explicit decision for a deterministic regression and one dead-PID exit-status recheck, overlay only that helper correction, then rerun n4 from zero before n7 |

- 2026-08-16 | `M5` | issue #15 fail-closed host actions | made every operator service-start and resource-sampler start/stop action propagate its first host failure immediately instead of allowing later host success to mask it | correct the exact orchestration defect proven by rejected n4 resource p1 without changing frozen protocol identities | final lifecycle/helper test, frozen execution overlay, EC2 runbook, validation/status docs, this changelog, and issue #15 | the focused regression failed against all three masked loops before the correction; complete lifecycle, final contract, remote-job, Terraform, runner-portability, 29 artifact, race-gate contract, and full node/BTE/mempool race suites pass; n4/n7 resource `--validate-only` passes and task/frozen lifecycle SHA-256 is `0ae2371303f027bd226d273c337870d42a9b1490170ac78d780a50fa70026296` | execute n4 resource p2 from zero and require complete artifact plus authenticated cleanup acceptance before n7 |

- 2026-08-16 | `M5` | issue #15 rejected n4 resource p1 | retained invalid `bloc-ec2-i15-sa-n4-resource-p1` after a masked operator service-start failure kept node 0 at HTTP 503, then completed fresh authenticated cleanup | preserve the failed evidence and stop before an unchanged rerun whose per-host service and sampler loops can mask an earlier SSH failure | ignored p1 artifact, `docs/STATUS.md`, this changelog, and issue #15 | provisioning created the expected 15 resources and staging plus exact image verification passed; operator 1's service-start SSH call timed out, later host success masked it, no sampler or evaluator measurement ran, and recovered node-0 logs show repeated failure to dial the absent peer; Terraform destroyed all 15 resources and deleted the key; after a transient credential rejection in the wrapper cleanup event, a fresh authenticated check found no instances, volumes, VPC/network objects, key pair, IAM role/profile, or Terraform state | obtain an explicit decision for fail-closed per-host service and sampler start/stop propagation with regressions and an exact frozen overlay, then run n4 resource p2 from zero before n7 |

- 2026-08-12 | `M5` | issue #15 accepted n7 same-AZ latency | completed and accepted from-zero campaign `bloc-ec2-i15-sa-n7-latency-p6` with 3,000 measured attempts under the frozen final-campaign contract | finish the missing n7 half of the primary same-region p99 evidence without mixing latency and resource collection | ignored p6 artifact, `docs/STATUS.md`, this changelog, and issue #15 | all 1,000 attempts per batch were successful, consistent, and within 12 seconds across 10 balanced blocks; Type-7 total-slot p50/p95/p99 was 143.081/156.662/301.749 ms at batch 8, 388.567/484.247/1,026.110 ms at batch 32, and 1,730.534/2,064.372/3,232.609 ms at batch 128; manifest provenance, final-phase validation, lifecycle recovery, and authenticated cleanup pass; the run took 3h10m06s, Terraform destroyed all 18 resources, state and direct EC2/IAM queries are empty, and no local key remains | obtain separate live authorization for the n4/n7 resource phases, then validate the complete matched issue #15 artifact set |

- 2026-08-12 | `M5` | issue #15 bounded image distribution | gave each exact digest-pinned image operation at most three attempts over bounded SSH and made every operator/controller verification fail immediately instead of allowing a later loop success to mask it | recover from p5's transient controller reachability loss while preserving fail-closed immutable ECR distribution | final lifecycle/helper test, frozen execution overlay, EC2 runbook, validation/status docs, this changelog, and issue #15 | the recovery regression first failed after the current one-shot attempt and the masking regression first failed after the initial operator error; both pass after the minimal correction, along with the complete lifecycle and topology suites, final contract, 29 artifact tests, race-gate contract, runner portability, Bash syntax, both Terraform validations, diff hygiene, task/frozen helper equality at SHA-256 `ac64399ec838e82ca70bfb183fa5e41026b758d9c1366e97ead3139fce8fb666`, and exact frozen n7 p6 same-AZ `--validate-only`; no AWS API, Terraform plan/apply, ECR write, EC2 creation, Git push, or p6 live execution occurred | obtain separate live authorization for a from-zero n7 latency p6 |

- 2026-08-10 | `M5` | issue #15 rejected n7 latency p5 | retained invalid `bloc-ec2-i15-sa-n7-latency-p5` after a transient controller SSH loss during final digest-pinned image verification, before service startup or measurement, then completed recovery and authenticated teardown | preserve fail-closed evidence and stop before silently changing the frozen campaign tooling or relaunching | ignored p5 artifact, `docs/STATUS.md`, this changelog, and issue #15 | p5 provisioned eight expected `t3.small` instances in `us-east-1a` with 8 GiB operator and 16 GiB controller volumes and passed materialization/staging; the controller was reachable before the one-shot image pull, did not answer its final SSH verification, and was reachable again during recovery about one minute later; no service or measurement began; Terraform destroyed all 18 resources, state and authenticated cleanup are empty, both temporary keys are absent, and the independent cleanup validator passes; inspection found that per-host image pulls have no retry and earlier loop failures can be masked by later successes | obtain an explicit decision for at most three idempotent digest-pull retries plus immediate per-host failure propagation, validate locally, and request separate authorization before a from-zero p6 |

- 2026-08-10 | `M5` | issue #15 p5 storage and recovery readiness | fixed the final-campaign controller root volume at 16 GiB in both topologies while leaving operator volumes unchanged, added bounded rsync transport and phase-aware resource recovery, and made post-measurement lifecycle events report independent outcomes | prevent p4's controller disk exhaustion and nine-hour latency-phase resource-copy hang while preserving trustworthy cleanup evidence | same-AZ/three-region Terraform and adapters, final lifecycle/test, canonical runbook/validation/status, frozen execution overlay, and issue #15 | regression first failed on the missing rsync bound; focused lifecycle and both adapter contracts pass; both Terraform modules format and validate directly; static Terraform contract, 29 artifact tests, race-gate contract, full runner portability, diff hygiene, and task/frozen SHA-256 equality for all seven execution files pass; exact frozen n7 p5 same-AZ and matching three-region `--validate-only` pass without AWS access | obtain separate authorization for a from-zero n7 same-AZ latency p5 |

- 2026-08-07 | `M5` | issue #15 rejected n7 latency p4 | retained invalid `bloc-ec2-i15-sa-n7-latency-p4` after the controller's 8 GiB root volume filled in the final block-10 batch-128 job, recovered its durable job/output evidence, terminated only a nine-hour-stalled optional resource `rsync`, and completed authenticated teardown | preserve fail-closed evidence and stop before silently changing frozen campaign infrastructure or rerunning a partial schedule | ignored p4 artifact, `docs/STATUS.md`, this changelog, and issue #15 | p4 passed provisioning through health; 29/30 unique reconnectable jobs exited zero, the last retained 74/100 measurements and a truncated JSON record with an empty exit-status file, yielding 2,974 complete measured records and 26 missing; recovered evaluator output is about 4.7 GiB; Terraform destroyed all 18 resources and deleted the key, independent cleanup validation passes, state is empty, and no PEM remains; lifecycle destroy/cleanup labels are misleadingly failed because they inherit cumulative measurement status | obtain an explicit decision for controller-only storage headroom, bounded or skipped optional recovery, and accurate per-stage labels; validate locally, then request separate authorization for a from-zero p5 |

- 2026-08-06 | `M5` | issue #15 reconnectable evaluator jobs | replaced foreground evaluator SSH execution with atomically claimed controller-local jobs, idempotent bounded start, reconnectable bounded status polling, durable command/PID/stdout/stderr/exit state, and job artifact recovery | prevent p3's lost SSH channel from aborting or unsafely duplicating remote evaluator work | remote-job helper/test, final lifecycle/test, runner suite, canonical runbook/validation/status, focused design/plan, frozen overlay, and issue #15 | duplicate-start, lost-response, transient-poll, exhaustion/reattach, nonzero, ambiguous, lost, staging, recovery, measurement failure, and exact slot-allocation regressions pass; helper/lifecycle/contract, 29 artifact tests, Bash syntax, diff hygiene, and full runner portability pass without changing evaluator arguments or frozen protocol identities; helper/lifecycle hashes match the frozen overlay and exact n7 p4 validate-only passes | obtain separate live authorization for n7 p4 |

- 2026-08-06 | `M5` | issue #15 rejected n7 latency p3 | retained invalid `bloc-ec2-i15-sa-n7-latency-p3` after its foreground controller SSH channel timed out during block 2, then authenticated empty cleanup | preserve the incomplete schedule and stop before an unsafe blind retry or downstream resource launch | ignored p3 artifact, `docs/STATUS.md`, this changelog, and issue #15 | p3 passed provisioning, all seven previously blocked corpus transfers, exact images, startup, and health; block 1 retained 300 measured rows and block 2 retained complete batch-32/128 cells for 500 measured rows total, all successful and consistent; the recovered batch-128 file contains all 100 block-2 rows although terminal output stopped after row 71, proving the remote command completed across the lost foreground SSH channel; Terraform destroyed 18 resources, independent cleanup validation passes, state is empty, and no private key remains | decide between one unchanged from-zero relaunch and a separately approved reconnectable remote-job tooling correction; do not start resource phases first |

- 2026-08-06 | `M5` | issue #15 bounded n7 staging retry | gave final-campaign SCP the existing SSH connection/server-alive bounds and at most three transfer attempts, with no source, image, corpus, schema, schedule, or protocol change | recover from the transient one-shot corpus-transfer resets that invalidated n7 latency p1/p2 while still failing closed after a small fixed bound | final lifecycle helper/test, frozen execution overlay, `docs/STATUS.md`, this changelog, and issue #15 | the recovery regression first failed against the one-shot helper, then focused lifecycle, final contract, 29 artifact tests, Bash syntax, diff hygiene, and the complete runner portability suite passed; task/frozen helpers have identical SHA-256 `4dca0b062833b8204955dd1bacc691e3df2703efe9d41616e1361bb6e81ca0c5`; n7 p3 `--validate-only` passed the fixed latency contract | execute n7 latency p3, authenticate empty cleanup, then run the separate n4/n7 resource phases sequentially |

- 2026-08-05 | `M5` | issue #15 rejected n7 latency staging attempts | retained invalid `bloc-ec2-i15-sa-n7-latency-p1` and p2 before measurement after staging failed, then authenticated empty cleanup for both | preserve fail-closed evidence and stop repeated full-topology launches at a proven one-shot transfer boundary | ignored p1/p2 artifacts, `docs/STATUS.md`, this changelog, and issue #15 | p1 applied the expected 18 resources and lost its SCP connection while copying the immutable corpus to operator 4; p2 applied 18 resources after three instances took about 17 minutes to become ready and again recorded `stage=failed`; both Terraform states contain zero resources, both cleanup documents pass the independent validator, and no temporary key remains; the frozen `final_scp` helper has no bounded connection/server-alive options or retry while `final_ssh` does | obtain explicit approval for a three-attempt bounded SCP tooling correction and regressions before a third n7 latency launch |

- 2026-08-05 | `M5` | issue #15 block-scoped final attempt identity | changed final artifact uniqueness from bare `run_id` to `(measurement_block, run_id)`, added a real 10-block regression that also rejects a duplicate pair, and revalidated the retained n4 same-AZ latency artifact | evaluator invocations intentionally restart IDs within each balanced block, so the validator's global-ID assumption rejected complete evidence without identifying a missing observation | final artifact validator/tests, frozen tooling overlay, `docs/VALIDATION.md`, `docs/DECISIONS.md`, `docs/STATUS.md`, this changelog, and issue #15 | the regression failed at `batch 8: expected 1000 retained attempts` before the correction; 29 artifact tests, lifecycle/contract/runner suites, corrected final-phase validation, and authenticated cleanup validation pass; each batch retains 1,000 unique block-scoped identities and 1,000 successful, consistent, deadline-met attempts across blocks 1--10; Terraform state and authenticated cleanup are empty | collect n7 latency, then separate n4/n7 resource phases under the unchanged source, images, corpus, schedule, and protocol semantics |

- 2026-08-03 | `M5` | issue #15 accepted p11 readiness pilot | executed the frozen n4 same-AZ readiness phase with monotonic slots and retained 9/9 successful, consistent, deadline-met measurements | prove the repaired final lifecycle is ready for primary same-region collection | ignored p11 artifact, readiness plan, `docs/STATUS.md`, this changelog, and issue #15 | batch-8 totals were 89.350--99.365 ms, batch-32 totals were 312.324--319.261 ms, and batch-128 totals were 1.625--1.698 s; slots advanced through 1--4, 5--8, and 9--12 including warmups; manifest, evaluator, phase, provenance, log, and cleanup checks passed; Terraform destroyed all 15 resources, authenticated queries/state are empty, and the cloud/local key is absent | obtain separate authorization for primary same-AZ n4/n7 latency and dedicated resource phases |

- 2026-08-03 | `M5` | issue #15 monotonic campaign slots | carried one `next_slot` across every final-campaign batch/block evaluator invocation using the established A1/M3 pattern | prevent persistent nodes from rejecting later scenarios that reused already terminalized slot numbers | final lifecycle/test, frozen execution overlay, readiness plan, `docs/STATUS.md`, this changelog, and issue #15 | the new readiness/full-primary regression failed because `--first-slot` was absent, then passed with readiness starts 1/5/9 and the complete 30-invocation primary sequence through 2931; full lifecycle/topology, 28 artifact tests, contract, runner portability, both Terraform validations, exact-overlay comparison, and p11 `--validate-only` passed | obtain separate authorization before executing live p11 |

- 2026-08-03 | `M5` | issue #15 fail-closed p10 retry | corrected controller output ownership, evaluator error propagation, mandatory artifact validation, recovery environment, and local-key cleanup, then retained complete negative p10 readiness evidence | prevent empty or operationally incomplete phases from being accepted before primary collection | final lifecycle/topology adapters/tests/plan, ignored p10 artifact, `docs/STATUS.md`, this changelog, and issue #15 | five independent regressions failed before implementation; targeted/full lifecycle, topology, artifact, contract, and Terraform checks passed afterward; p10 passed every operational gate and retained nine measured attempts, with 3/3 batch-8 successes and 0/3 batch-32 plus 0/3 batch-128 because each evaluator invocation reused slots 1-4 after batch 8 terminalized them; mandatory validation accepted the complete negative artifact, all 15 AWS resources and the cloud/local keys were removed, authenticated queries/state are empty, and recovery logs are usable | obtain approval to add monotonic cross-invocation slot allocation using the existing A1/M3 pattern before p11 |

- 2026-08-03 | `M5` | issue #15 jq/readiness retry | installed `jq` explicitly on both EC2 topology variants, proved every p9 health gate, and retained invalid p9 after the controller evaluator produced no artifacts | satisfy the existing eight-item readiness check without changing frozen protocol identities | EC2 user data, lifecycle test/plan, ignored rejected p9 artifact, `docs/STATUS.md`, this changelog, and issue #15 | the user-data regression failed before implementation and lifecycle/topology plus Terraform checks passed afterward; p9 passed provisioning through health, then frozen runtime UID `10001` could not write below the host-owned controller results bind mount; measurement SSH failures were swallowed, artifact validation was disabled, the manifest was incorrectly `complete`, recovery omitted Compose `NODE_ID`, and local key cleanup was incomplete; no measurement exists; all 15 AWS resources and the AWS key were removed, authenticated queries/state are empty, and the exact local key was manually deleted | obtain approval for the bounded controller ownership, fail-fast measurement, mandatory artifact validation, recovery environment, and local-key cleanup corrections before p10 |

- 2026-08-03 | `M5` | issue #15 loopback-health retry | exposed mempool port 8080 only on host loopback, retained invalid p8, and stopped at a newly proven operator-host package dependency | align the existing host readiness probe with Compose without exposing mempool through the VPC or changing frozen protocol identities | operator Compose, lifecycle test/plan, ignored rejected p8 artifact, `docs/STATUS.md`, this changelog, and issue #15 | the resolved-Compose regression failed before implementation and all three lifecycle variants passed afterward; p8 passed frozen validation, provisioning, staging, exact image verification, and startup, and the loopback probe reached mempool, but the operator host lacks `jq` required by the existing eight-item response check; no measurement began; Terraform destroyed all 15 resources, both temporary keys were deleted, and authenticated resource queries plus state are empty | obtain approval to install `jq` explicitly in both EC2 user-data variants and retry p9 |

- 2026-08-02 | `M5` | issue #15 operator-secret retry | assigned only the staged operator secret to frozen runtime UID/GID `10001:10001` while retaining mode `0600`, retained interrupted p6 and invalid p7, and stopped at a newly proven health-check mismatch | make the private secret readable without weakening it or changing any frozen protocol identity | final lifecycle/tests/plan, ignored rejected p6/p7 artifacts, `docs/STATUS.md`, this changelog, and issue #15 | the ownership regression failed before implementation and all three lifecycle variants passed afterward; p7 proved `10001:10001 0600`, exact image pulls, two running containers per operator, and healthy mempool logs, but the gate probes host `127.0.0.1:8080` while Compose publishes no host port; no measurement began; all exact p6/p7 AWS resources and keys were removed, authenticated queries and Terraform states are empty | obtain approval for one loopback-only `127.0.0.1:8080:8080` Compose binding and retry in a persistent terminal |

- 2026-08-02 | `M5` | issue #15 container-input retry | exposed the frozen config's canonical CRS path read-only, staged public config/corpus inputs with mode `0644`, bounded SSH connection attempts, and retained invalid attempt `bloc-ec2-i15-sa-n4-p5` | correct the two p4 container boundaries without weakening the private operator secret or changing frozen protocol identities | operator Compose, final campaign lifecycle/tests/plan, ignored rejected artifact, `docs/STATUS.md`, this changelog, and issue #15 | red/green staging, real Compose, and SSH regressions plus same-AZ/three-region lifecycle contracts passed; p5 created 15 resources and passed staging, exact digest verification, and startup; mempool became healthy, while BLOC restarted because the mode-`0600` operator secret remained host-owned and unreadable by frozen runtime UID/GID `10001:10001`; Terraform destroyed all 15 resources, both temporary keys were deleted, authenticated resource checks and state are empty, and no measurement exists | obtain approval to chown only the staged secret to `10001:10001` while retaining mode `0600`, then run p6 |

- 2026-08-02 | `M5` | issue #15 bounded-readiness retry | added a 60-attempt post-start health gate and interpolatable failure-log recovery, then retained interrupted attempt `bloc-ec2-i15-sa-n4-p4` after live logs proved two deployment-boundary failures | distinguish slow BMax-128 initialization from deterministic container startup failures without changing frozen protocol identities | final campaign lifecycle/tests/plan, ignored rejected artifact, `docs/STATUS.md`, this changelog, and issue #15 | red/green health and recovery regressions plus same-AZ/three-region lifecycle contracts passed; p4 created 15 resources and reached Compose startup, but BLOC restarted because the CRS was mounted at `/config/cluster.ec2.crs` instead of required `/config/cluster.crs`, while mempool restarted because its non-root user could not read the mode-0600 corpus; Terraform destroyed all 15 resources, both temporary keys were deleted, authenticated EC2/IAM checks and state are empty, and no measurement exists | obtain approval to correct the frozen Compose mount target and public artifact permissions before another pilot |

- 2026-08-02 | `M5` | issue #15 post-start readiness pilot | added bounded cloud-init/Docker host readiness, fail-fast staging, and controller directory ownership; retained provider-only attempt `bloc-ec2-i15-sa-n4-p2` and invalid live attempt `bloc-ec2-i15-sa-n4-p3` | make staging deterministic while preserving the frozen protocol source, images, bundle, and corpus | final campaign lifecycle/tests, ignored rejected artifacts, `docs/STATUS.md`, this changelog, and issue #15 | focused shell, topology, Terraform, and artifact tests passed; p2 applied no resources after a transient provider download failure; p3 reused the exact locked provider, created the expected 15 resources, passed staging and image/service startup, then failed the immediate single-attempt health gate before measurement; Terraform destroyed all 15 resources and the key, and authenticated cleanup plus state are empty | obtain approval to add a bounded post-start health retry and pass the immutable image variables to Compose log recovery before another pilot |

- 2026-08-02 | `M5` | issue #15 short-ID same-AZ readiness pilot | bounded final campaign IDs to 47 characters, corrected the shared empty-cleanup assertion, and retained invalid attempt `bloc-ec2-i15-sa-n4-p1` after controller staging failed before measurement | keep the fix small while rejecting known-invalid names and cleanup results before the primary campaign | final campaign contract/lifecycle/adapters/tests/runbook, ignored rejected artifact, `docs/STATUS.md`, this changelog, and issue #15 | focused shell regressions, both topology adapter contracts, Terraform contract tests, and 28 artifact tests passed; the live apply created 15 expected resources, then staging exposed missing controller directory privileges and an operator transfer reset before cloud-init completion; Terraform destroyed all 15 resources and the key, authenticated cleanup and state are empty, and no metric observation exists | add a bounded cloud-init readiness gate and correct controller directory ownership before the next short-ID retry |

- 2026-08-02 | `M5` | issue #15 second rejected same-AZ readiness pilot | verified the updated deployer policy on IAM user `bloc`, retained invalid attempt `bloc-ec2-issue-15-same-az-n4-pilot-20260802T150441Z` after Terraform rejected its derived 68-character IAM role name, and exposed a mis-grouped frozen jq cleanup assertion that rejects an authenticated empty cleanup document | preserve the frozen-candidate boundary and do not launch a retry whose artifact lifecycle is known to fail closed | ignored rejected artifact, `docs/STATUS.md`, this changelog, and issue #15 | `ListRoles` and `ListInstanceProfiles` passed; validate-only passed; Terraform applied zero resources; the temporary key was deleted; authenticated AWS queries and Terraform state returned empty for every scoped resource class; the independent cleanup validator passed | obtain an explicit frozen-tooling invalidation decision, correct the cleanup assertion, then retry with a `bloc-ec2-*` experiment ID of at most 47 characters |

- 2026-08-02 | `M5` | issue #15 rejected same-AZ readiness pilot | retained invalid attempt `issue-15-same-az-n4-pilot-20260802T144509Z` after Terraform stopped before instance creation on `iam:CreateRole` denial, then destroyed all seven partial network resources and the temporary key | do not conceal an authorization-contract failure or accept a pre-measurement attempt as protocol evidence | ignored rejected artifact, `docs/STATUS.md`, this changelog, and issue #15 | direct authenticated checks returned no scoped instances, volumes, VPCs, subnets, security groups, route tables, internet gateways, or key pairs; Terraform state contains zero resources; no metric observation exists; cleanup's IAM queries also exposed missing `iam:ListRoles`/`iam:ListInstanceProfiles` | reconcile the accepted `bloc-ec2-*` IAM namespace, inline pull-policy scope, and global read-only cleanup permissions before retrying from the unchanged frozen source |

- 2026-08-02 | `M5` | issue #15 replacement campaign freeze | froze clean source `cf36eb06bea12eb3b0fcfdfaf94a349c2dbe784f`, created two shared immutable scan-on-push private ECR repositories, built and published the BLOC and mempool `linux/amd64` images, pulled both exact digests back for runtime inspection, and bound them into final n4/n7 manifests | make the first final VM campaign executable from one distributable source/image/corpus identity while allowing issue documentation to advance separately | ignored private bundles/manifests, detached frozen-source worktree, `docs/STATUS.md`, this changelog, and issue #15 evidence | BLOC digest `sha256:a58d8ef4ef5a674ce89341538798b47a422ffdc66d72637d8b3f4351282a2eec` and mempool digest `sha256:3c0c147a92d66c89293f9bda89967bded2ae22795bd37de09fa466ca4dbe38aa` resolved from ECR; digest pulls and `linux/amd64`/non-root/entrypoint inspection passed; n4/n7 bundle verification, all ten side-effect-free final runner permutations, regional 16/16/16 vCPU quotas and fixed-AZ `t3.small` offerings passed; the detached-source normal and split race gate passed | obtain separate authorization for the same-AZ n4 readiness pilot; no Terraform plan/apply or EC2 resource was created during the freeze |

- 2026-08-02 | `M5` | issue #15 local race gate | added one automated split gate that runs all deterministic BMax-128 bundle/materialization tests normally, races BTE and mempool directly, and races every other bloc-node application test with an exact-name exclusion list | package-wide bloc-node race runs spent 10 and 30 minutes repeatedly parsing CRS/pairing material without reporting a race, making that command unsuitable as a routine readiness gate | `scripts/tests/run-final-campaign-race-gate.sh`, its red/green shell contract, validation/status/readiness-plan docs | complete bloc-node application tests passed in 203 seconds, BTE race in 35 seconds, mempool races in 85 seconds, remaining bloc-node races in 70 seconds, 37 chart tests and all campaign regressions passed; no production code, AWS, registry, Terraform lifecycle, or push operation changed | freeze/publish/inspect the replacement source and two images, bind final n4/n7 manifests, then request separate n4 pilot authorization |

- 2026-08-02 | `M5` | issue #15 final VM campaign readiness | added frozen n4/n7 campaign identities and bundle verification, offline corpus binding, topology materialization, fixed pilot/latency/resource contracts, pull-only shared-ECR Terraform, checksum/architecture/health gates, recovery, fail-closed artifact validation, and same-AZ/three-region lifecycle adapters with authenticated cleanup | make the matched final VM campaigns automated and provenance-identical without runtime cryptography, synthetic fallback, image publication, or mixed latency/resource sampling | node/mempool campaign commands, final runners/lifecycle/adapters, both Terraform modules, artifact validators/tests, canonical runbooks and status | four ordinary Go suites, BTE/mempool focused races, 28 artifact tests, campaign contracts, both Terraform validations, Compose resolution, real n4/n7 corpus bundle verification, and all ten primary validate-only permutations passed; chart dependencies were unavailable and the full bloc-node race suite timed out during active BTE verification; no AWS, registry, push, or Terraform plan/apply occurred | complete the two outstanding local checks, freeze/publish/inspect the replacement source and images, bind final manifests, then request separate n4 pilot authorization |

- 2026-07-29 | `M5` | issue #15 ciphertext and workload replacement | replaced slot-bound hybrid ciphertexts with capsule-authenticated `bte-tx-v2`, advanced public configs to `bloc-cluster-v3`, added the nested 512-target corpus, offline cluster-specific encryption/self-check, immutable prefix serving, exact evaluator proposal limits and provenance, and fail-closed no-AWS deployment preflight | make repeated realistic ciphertext prefixes reusable across slots and comparable across batch occupancies without runtime encryption or synthetic fallback | BTE, mempool, node/evaluator, Compose/EC2 preflight, canonical architecture/validation/status/decision docs | all affected Go suites and focused races passed; Compose resolved with digest-addressed images; shell syntax and local preflight contracts passed; no AWS or push occurred | freeze replacement source/images/artifacts only after immutable distribution and authenticated cleanup are implemented and rerun the full release-candidate validation |

- 2026-07-28 | `M5` | issue #8 distributed-campaign preflight | accepted the frozen release candidate's validation-only local matrix after authorizing a 60-second harness timeout for BMax-512 initialization while keeping the 12-second slot deadline unchanged; retained 6/6 primary and 18/18 extension measured observations as successful and consistent, preserved the rejected 30-second pre-slot attempt, and produced the aggregate manifest, report, and diagnostic charts | prove every planned distributed-campaign configuration and artifact boundary before allocating VM resources without promoting local timings or resources as thesis evidence | ignored `results/local/distributed-preflight-2bc8efc/`, `docs/STATUS.md`, `docs/VALIDATION.md`, this changelog, and the issue #8 execution plan | four exact-source Go suites, 37 chart tests, campaign-runner and side-effect-free Terraform validation, exact attempt counts, primary deadline coverage, schema/provenance/per-node coverage, timing additivity, CSV aggregation, and chart-loader/render compatibility passed; no resource, AWS, image publication, or Git push operation ran | make issue #15 the first M5 thesis-performance campaign, followed by issue #16 with matched manifests and configurations |

- 2026-07-27 | `M5` | evidence strategy and tracker ordering | approved [issue #8 distributed-campaign preflight design](superpowers/specs/2026-07-27-issue-8-distributed-preflight-design.md), re-scoped issue #8 from a local p99 campaign to validation-only contract preflight, selected M5, and ordered issue #15 before issue #16 for final VM performance/resource evidence | final sidecar performance and resource evidence is VM-per-operator only; local evaluation proves frozen-contract execution and artifacts without thesis-performance claims | `docs/STATUS.md`, `docs/ROADMAP.md`, `docs/VALIDATION.md`, `docs/DECISIONS.md`, this changelog, thesis evaluation plan, GitHub issues #8/#15/#16 and Project fields | documentation/tracker coherence, local Markdown links, stale-reference scan, and `git diff --check` | execute issue #8 preflight; on success, issue #15 is the next thesis-performance task |

- 2026-07-26 | `M4` | release-candidate validation and Compose rehearsal | restored the root Docker build context for `mempool-il`, preserved split operator-secret generation in the mock-placeholder Compose overlay, added regression assertions, corrected the mock rehearsal batch-size command, and froze source `2bc8efc9269798a7f7ab58021f8b9bda1012ae5d` with `linux/amd64` image digest `sha256:ee99ceb095e241fb75af930e5b2c0674ba2fa32f63abba754882aa5611f7b754` | make all final thesis campaigns start from one locally rehearsed source/image/configuration/schema contract | Docker context, mock Compose overlay/runner regression gate, Compose runbook, roadmap/status/validation docs | four Go suites and 37 chart tests; full/targeted races; two 30-second decoder fuzz targets; Terraform and runner validation; local evaluator/demo; BTE full-path 8/32/128/512; ACS 1,000-schedule, 100-slot, 180-slot and attribution gates; standard and encrypted-corpus Compose evaluation with 4/4 healthy targets and exact 100-target materialization | explicitly select M5, then run final campaigns without mixing source or image revisions |

- 2026-07-26 | `M4` | `mempool-il` client-overhead methodology | separated the balanced client sampling frame from the realistically weighted full-protocol workload, added a deterministic 500-target client corpus with 100 unique signed transactions per payload class, retained the separate `28/50/12/8/2` mock-placeholder protocol corpus, and made the client report reject cycling and sample counts other than 100 per class | isolate payload size as the client experiment's independent variable while reserving observed Ethereum transaction-size proportions for ACS/BTE workload construction | client and protocol corpus contracts/fixtures, client report tests and selection, issue design/amendment plan, mempool README/deep dive, validation guide | the new corpus readers failed before implementation; exact-count and incomplete-class tests failed against the cycling implementation; focused corpus/report/CLI tests passed after the changes | regenerate and bind the ignored client-overhead artifact, then validate and freeze issue `#14` |

- 2026-07-26 | `M4` | `mempool-il` client-overhead evidence | replaced the four-row replay fixture with 100 deterministic valid signed chain-1337 EIP-1559 transactions weighted `28/50/12/8/2` across transfer and 128/256/1,024/4,096-byte calldata classes, added strict class/signature/hash/gas validation, split the replay encryption boundary, and added an atomic CSV report with 100 plaintext/encrypted measurements per class and a documented EIP-7623 carrier estimate | provide defensible RQ2/RQ4 client preparation-latency and byte-expansion evidence while keeping the mainnet workload-share rationale explicit and avoiding live submission or paid-gas claims | committed corpus, `mempool-il` corpus/replay/report code and tests, `cmd/corpus-report`, module README/deep dive, validation/status, issue design and plan | red tests established the corpus, split-encryption, schema/count/order, serialization, gas-floor, CLI, and atomic-write contracts; full `mempool-il` and `bloc-node` suites and targeted mempool race tests passed; ignored `results/issue-13-client-overhead/client_overhead.csv` contains 500/500 valid rows, 100 per class, SHA-256 `e9b6e77c28328f640e0e8fc98c53036207c26911f58c21b5a65a7493b9bc9d2e` from source `71c3584328bffd55bd2886bc7098839110d5fe1a` | validate and freeze issue `#14` before collecting final M5 campaigns |

- 2026-07-26 | `M4` | evaluator statistics and campaign scheduling | added evaluator schema v3 with attempted/completed/deadline/failed/timed-out outcome accounting, a 1,000-successful-observation p99 gate, pandas Type-7 p50/p95/p99 reporting with exact non-parametric 95% order-statistic intervals, stable seeded balanced measurement blocks with durable plan/order metadata, and guarded `n=10`/batch-512 local, same-region, and three-region paths; retained terminal attempts as complete negative evidence while excluding them from latency distributions | make the final honest-path and scale campaigns statistically auditable without hiding failure rates or publishing underpowered p99 claims | `bloc-node` evaluators/tests/README, latency chart statistics/data/reports/tests, active EC2 runners and artifact helpers/tests, validation/status/planning docs | full `bloc-node` Go suite passed; 37 chart tests passed; 24 artifact fixtures and every side-effect-free campaign-runner validation path passed; `git diff --check` passed; no AWS operation was performed and no p99 campaign evidence or baseline was accepted | complete issue `#13`, validate and freeze issue `#14`, then run final campaigns only after explicit authorization |

- 2026-07-26 | `M4` | EC2 resource evidence | added a non-contaminating 250 ms host-local cgroup-v2/Docker-fallback sampler, dedicated same-region/three-region resource phases, strict cadence/coverage/counter/health validation, complete scenario/node coverage, bounded live-sampler shutdown, and per-node/configuration/cluster summaries; preserved historical M3 coarse restart/OOM/stability analysis without fabricating a host summary | collect operator resource evidence without perturbing primary latency/p99 phases or conflating host network counters with protocol-message bytes | `deploy/ec2/sample-container-resources.sh`, active EC2 runners, artifact and three-region analysis/tests, runbook, validation/status/planning docs | 21 artifact fixtures, campaign-runner portability checks, and 28 chart tests passed for source `699f2ad264fc394668c59dfa69f882711ffbac16`; no AWS operation was performed and no live resource evidence is accepted by this source-only validation | complete M4 issues `#12` and `#13`, freeze issue `#14`, then run an explicitly authorized release-candidate resource campaign |

- 2026-07-24 | `M4` | bloc-node mempool HTTP provider | added `provider.mempool_timeout_ms`, normalized omitted/zero values to 2,000 ms, rejected negative and duration-overflowing values, emitted the setting from local and EC2 config generators, and replaced the global default client with one bounded client per node and a cancellable single-attempt request | prevent a stalled `mempool-il` service from blocking proposal preparation indefinitely while preserving successful and ordinary HTTP-error behavior | bloc-node provider/config/node/CLI code and tests, module README/deep dive, validation/status/decision/planning docs | the unbounded implementation failed the new contract tests; focused success/error/cancellation/blocking/config/defaulting tests and the full bloc-node normal and race suites passed for source `1ecead9b7b1653f8b047bec28b7c45e59620e643` | complete M4 evidence tooling issues `#11`, `#12`, and `#13`, then validate and freeze issue `#14` |

- 2026-07-24 | `M4` | bloc-node slot lifecycle and evaluators | added an idempotent `failed` slot phase and bounded `SlotFailure`, made `/result` return stable HTTP 422 failures while preserving 202 pending/200 success/409 wrong-slot semantics, routed synchronous `/start` failures through that result boundary, preserved peer-driven terminal success/failure across local-start ordering, allowed failed slots to prepare forward, prevented late success from replacing failure, and made all evaluators retain run-level failure rows/reasons outside latency summaries | let controllers and RQ3 campaigns distinguish deterministic protocol failure from pending work or client timeout without leaking dynamic errors or accepting failed observations as latency evidence | `bloc-node/internal/app` lifecycle, metrics, evaluator polling/artifacts, tests, architecture/module/validation/status/decision/planning docs | focused red/green lifecycle, start-ordering, evaluator, and artifact tests plus full bloc-node normal and race suites passed for source `5b781714d59bd9b9c66e8a3bc7abb651fa404093` | complete the mempool timeout and remaining evidence-tooling issues before freezing M4 |

- 2026-07-23 | `M4` | HBBFT reliable broadcast | bound Reed-Solomon reconstruction to ECHOs matching the selected root, delayed terminal decode state until successful reconstruction, recomputed the Merkle commitment before output, added mixed-root, duplicate-index, retry, and wrong-root regressions, and made the ACS evidence runner hash and race-test RBC | prevent Byzantine-proposer/root-equivocation schedules from producing payloads that were not authenticated by the READY/ECHO threshold root and bind retained acceptance evidence to the changed implementation | `sbc/hbbft/rbc.go`, `sbc/hbbft/rbc_test.go`, ACS safety runner/contract tests, HBBFT/validation/status/PIR documentation | red tests reproduced a mixed payload and terminalized retry; clean source `ebb69c53e593309fa8c124aad35b80bd9c50f640` passed targeted and full HBBFT/bloc-node suites, the complete `acs-safety-issue9-ebb69c5` campaign (`100/100` sustained n4 and `180/180` n4/n7 measured slots), identity suites, the retained Merge/Plan benchmark, and the exact RBC/ACS/BBA race selection under Linux/arm64 in `results/local/rbc-mixed-root/ebb69c5/` | complete the remaining M4 terminal-failure and mempool-timeout blockers before freezing the evaluation release candidate |

- 2026-07-23 | `M4` | thesis evaluation planning and task management | reframed the four paper research questions around the independent BLOC sidecar boundary, selected M4 as the active evaluation-readiness milestone, defined the final p99 and fault/cost evidence contracts, reorganized M4–M7 around a two-week evidence programme, and made the repository-linked GitHub Project/issues the granular execution tracker | move from exploratory prototype development to a claim-driven path that produces defensible thesis results without implying Builder API or DVT integration | `AGENTS.md`, `docs/STATUS.md`, `docs/ROADMAP.md`, `docs/VALIDATION.md`, `docs/DEVELOPMENT.md`, `docs/DECISIONS.md`, detailed plan, GitHub project/milestones/issues | documentation consistency, local-link, project-field, milestone, and issue-coverage reviews | execute M4 blockers, freeze a release candidate, then collect M5–M7 evidence from the exact frozen source |

- 2026-07-19 | `not selected` | documentation and agent workflow | made `AGENTS.md` the sole mandatory agent entry point, rebuilt `STATUS.md` as concise live state, moved environment-specific commands into deployment runbooks, consolidated evaluation/demo/archive material into canonical owners, normalized repository-relative links, and removed superseded standalone documents | keep milestone, blocker, evidence, baseline, and next-action state coherent while reducing duplicated and stale agent context without adding documentation-checking code | `AGENTS.md`, root/module READMEs, `docs/`, `deploy/*/README.md`, removed `bloc-node` supplements and `depr/` archive | 162 local Markdown links resolved; stale-reference scans passed; all eight campaign/deployment entry points returned `--help` consistent with active examples; `git diff --check` passed; only documentation and the obsolete archive placeholder changed | keep `STATUS.md` current in every task covered by its maintenance contract; select the next active milestone only in a separately authorized planning task |

- 2026-07-19 | `M0` | repository Git ownership | made branch/status/divergence checks, focused commits, validation-before-integration, fast-forward preference, and safe pruning of fully merged task branches part of the agent operating contract; prohibited force-pushing and deletion of unmerged work | prevent long-lived task branches from accumulating unrelated milestones and make repository maintenance part of completing each task | `AGENTS.md`, `docs/DEVELOPMENT.md` | audited all local/remote refs and worktrees after a pruned fetch; proved the active 22-commit history directly descends from `main`, two task refs are already ancestors, and the remaining BTE attribution ref has five unique PowerShell-era commits that must be preserved for an explicit Bash port/review | validate the consolidated candidate, fast-forward and publish `main`, then prune only the fully merged task refs |

- 2026-07-19 | `M3` | accepted three-region latency evidence | recorded the completed `n=4/n=7`, batch `8/32/128` three-region campaign as the final M3 honest-path experimental baseline | replace the pre-deployment status with the evidence actually collected before moving to later thesis work | `docs/STATUS.md`, `docs/WORKFLOWS.md`, ignored accepted campaign artifacts | 180/180 measured slots and 990/990 finalized node rows passed; one image digest, placement, stage additivity, Prometheus, pairwise health, resource stability, and authenticated teardown gates passed; Terraform destroyed 40 then 43 resources | archive the evidence with checksums and decide whether to add a confirmation run or matched same-region control |

- 2026-07-18 | `M3` | three-region metric scope correction | removed CloudWatch CPU-credit queries, their CSV merge contract, and the proposed CloudWatch IAM permission; retained explicit T3 credit mode, detailed EC2 monitoring, protocol timing, pairwise HTTP timing, Prometheus readiness, restart/OOM sampling, and teardown evidence | the campaign objective is cross-region protocol latency, while AWS CPU-credit history is optional infrastructure telemetry and must not gate collection | three-region runner, deployer policy, canonical changelog | Bash syntax/artifact portability checks, JSON validation, and source scans passed; prior healthy probe teardown remained authenticated empty | freeze the corrected source and rerun the latency probe with the already-installed AWS policy |

- 2026-07-18 | `M3` | three-region deployer permissions | added `cloudwatch:GetMetricStatistics` to the deployer policy after the healthy second probe reached CPU-credit evidence collection | retain pre/post T3 credit and utilization evidence without broad CloudWatch mutation access | `deploy/ec2/bloc-three-region-deployer-policy.json` | second probe reached 4/4 healthy operators, then failed on the exact missing action; mandatory teardown destroyed all 40 resources and authenticated every scoped resource class as empty with cleanup exit 0 | replace the AWS policy once more and rerun the probe |

- 2026-07-18 | `M3` | three-region live-probe recovery | made every SSH invocation non-stdin-consuming so inventory loops configure and inspect every operator, assigned the mounted operator secret to the image's non-root UID with mode 0600, and separated phase failure from teardown failure in invalid evidence | the first fully authorized probe configured only its first streamed operator and that container could not read its secret, while its authenticated teardown actually succeeded but was mislabeled | `deploy/ec2/run-m3-three-region.sh`, campaign-runner portability checks | first probe destroyed all 40 resources and authenticated empty checks passed; corrected Bash/artifact suite and Terraform validation passed | freeze the corrected SHA and rerun the `n=4` probe before the accepted campaign |

- 2026-07-18 | `M3` | three-region campaign lifecycle policy | removed the billing-administrator confirmation gate after operator authorization, removed the three-region preserve-on-failure path, added three-attempt Terraform teardown and idempotent regional-key cleanup, retained authenticated empty-resource verification as a hard phase gate, and added the deployer policy with the required peering, monitoring, and scoped inline-role-policy actions | allow the metric campaign to proceed with accepted billing risk while making zero residual AWS resources the mandatory operational outcome | `deploy/ec2/run-m3-three-region.sh`, `deploy/ec2/bloc-three-region-deployer-policy.json`, canonical workflow/validation/status/decision docs | Bash syntax, artifact tests, campaign-runner portability validation, official AWS action-reference checks, and local JSON validation passed; the first live apply exposed missing deployer actions and then destroyed all 27 partial resources with empty Terraform state and regional cleanup checks | attach the corrected deployer policy, rerun the probe, verify empty teardown, then run and report the accepted `n=4`/`n=7` campaign |

- 2026-07-18 | `M3` | three-region EC2 deployment and evidence | replaced the active two-region `t3.medium` topology with `t3.small` in `us-east-1`, `eu-west-1`, and `eu-central-1`; added deterministic modulo-three placement, three full-mesh VPC peerings/six directional routes, hardened EC2 settings, repository-scoped IAM, exact Terraform allowlists, digest/credit/readiness/resource/cleanup contracts, region/AZ measurement attribution, pairwise network and critical-region reports, and Bash/Python fixtures | fit `n=7` within the verified 16/5/5 Standard On-Demand vCPU limits while collecting all EU/US pair classes | `deploy/ec2/terraform-three-region/`, `deploy/ec2/run-m3-three-region.sh`, `scripts/`, `latency-charts/`, canonical docs | Bash syntax/portability and 9 artifact tests passed; all four Go module suites and the full ACS safety campaign passed (100/100 sustained plus 180/180 matrix); 26 chart tests passed; Terraform formatted/validated and read-only plans passed at exactly 40 `n=4`/43 `n=7` additions; t3.small availability and 16/5/5 quotas were verified; the `linux/amd64` image built and inspected as amd64; no AWS resources created | commit/freeze the source, obtain administrator credit confirmation, run the `n=4` probe, then the accepted `n=4`/`n=7` campaign |

- 2026-07-17 | `M3` | cross-platform campaign tooling | replaced eight Windows PowerShell campaign entrypoints with Bash 3.2-compatible macOS/Linux runners, shared lifecycle/artifact helpers, resumable local phases, side-effect-free validation modes, explicit `linux/amd64` EC2 image builds, fixture-backed artifact checks, and canonical Bash workflow documentation | make local safety and distributed campaign automation executable from the current development host without maintaining two drifting implementations | local ACS/Merge+Plan runners, EC2 pilot/rerun/M3/attribution runners, shared scripts, canonical docs | macOS Bash 3.2 and disposable Linux Bash 5.2 portability suites passed; both Terraform modules formatted and validated; the complete local ACS campaign passed its 1,000-schedule, race, 100-slot, 180-run matrix, and identity gates; one current-code Merge/Plan baseline phase produced both profiles, attribution summaries, and 60/60 successful consistent measured evaluator runs; no AWS resources allocated | run one separately approved EC2 pilot before accepting new cloud evidence |

- 2026-07-17 | `M3` | BLOC resource-safety boundary | added compatible shared limits for 8 MiB proposals, 16 MiB inbound/outbound envelopes, and 256 cumulative recovery attempts per sub-batch; replaced append-only share state with authenticated per-operator candidates bounded by `N*BMax` before planning and `N*alpha` afterward; added deterministic cap-aware BTE combination and bounded accepted/rejected/attempt metrics | remediate the active resource-exhaustion portions of PIR-008/PIR-009 without changing protobuf, consensus, ciphertext, `BatchID`, or planning identities | `bloc-node` config/provider/transport/node/metrics, BTE cluster combine API, canonical docs | bloc-node and BTE module/race suites passed on the development host; exact/oversized proposal/envelope tests, membership/index/conflict/pruning tests, cap exhaustion, cumulative retry accounting, n10/t7 three-invalid-share recovery tests, persistent smoke, and the full local ACS campaign passed | rebuild an EC2 image only for a separately approved recovery probe |

- 2026-07-15 | `M3` | EC2 deployment, latency charts | added a dedicated `us-east-1`/`eu-west-1` private-peering Terraform topology and `t3.medium` cross-region campaign runner for `n=4/7`, preserved evaluator schemas, added pairwise network and CPU-credit evidence, strict distributed acceptance/cleanup checks, and an additive four-stage p50/p95 report | collect standalone current-build cross-region thesis latency without changing the accepted single-region runners or comparing incompatible commits as a controlled topology experiment | `deploy/ec2/terraform-cross-region/`, `deploy/ec2/run-m3-cross-region.ps1`, `latency-charts/`, canonical docs | Terraform initialized/formatted/validated; exact read-only plans passed with 26 `n=4` and 29 `n=7` allowlisted resources; PowerShell parsed; `bloc-node go test ./...` and all 24 chart tests passed; no AWS resources were created | run the quota-compatible `n=4` campaign; `n=7` requires increasing the `eu-west-1` Standard On-Demand vCPU quota from 5 to at least 6 |

- 2026-07-15 | `M3` | BTE setup, secret provisioning, operator transport | replaced shared setup-seed reconstruction with a versioned/hash-checked public CRS artifact, split public cluster configuration from per-operator BTE/libp2p secrets, rejected legacy combined configs, migrated local/Compose/EC2 launch paths, and bound envelope/share senders to authenticated configured libp2p peers | remediate the BLOC-owned portions of critical review findings PIR-001/PIR-002/PIR-004 without changing inherited RBC or diagonal-CRS logic | BTE PRF/BTD APIs, `bloc-node` config/node/transport/evaluators, replay-placeholder loader, active deployment workflows, canonical docs | public-CRS hybrid round trip, split-config, legacy rejection, and sender-spoofing tests added; all four module suites passed; Compose configuration resolved; local n4 BMax-128 batches 8/32/128 all succeeded consistently and emitted no config/secret artifacts | PIR-001 remains partially open until diagonal elements and setup ceremony receive a cryptographic design/review; cap inbound envelopes and bound share retention separately |

- 2026-07-15 | `M3` | protocol architecture documentation and source review | rewrote the root architecture as a top-down protocol map, added canonical deep dives for `bloc-node`, `mempool-il`, HBBFT, and BTE, consolidated README/agent routing, and recorded a source/paper/test-backed findings register | make every implemented protocol stage, identity, failure boundary, and paper deviation traceable without mixing deployment or campaign detail into the system architecture | `docs/ARCHITECTURE.md`, `docs/modules/`, `docs/archive/PROTOCOL_IMPLEMENTATION_REVIEW_2026-07.md`, module entry points and canonical routing docs | all four Go module suites passed; 21 changed Markdown files passed local-link resolution; 18 principal symbols were verified; `git diff --check` passed; all 7 pre-existing non-document changes remained byte-for-byte unchanged | address the critical RBC reconstruction, sender-authentication, setup/key-custody, BBA equivocation, and share-admission findings as separate correctness/security patches |

- 2026-07-15 | `M3` | Merge + Plan correctness boundary | added scope-bound BTE decode/plan APIs for the active cluster and slot, rejected malformed AEAD envelopes without allowing GCM panics, froze decoded batch identity and ownership, bound accepted inclusion lists to their ACS proposer and slot, and added a deterministic fallback for valid repeated-index layouts that the prior round-robin assignment rejected | establish a safe protocol baseline before parallel ciphertext decoding without changing wire formats, hashes, `BatchID`, `alpha`, or any previously successful plan | `bte/btd-impl-main/be`, `bloc-node/internal/app`, canonical docs | BTE and bloc-node Go suites passed; outer ciphertext fuzz passed 249,133 executions; capsule fuzz passed 917,044 executions; local n4 BMax-128 batches 8/32/128 all succeeded consistently with every selected count matching the batch size; Linux race validation remains unavailable on this Windows host because gcc, WSL, and a local Go Docker image are absent | run the two module race suites on Linux, then implement bounded parallel ciphertext decoding against this baseline |

- 2026-07-15 | `M3` | ACS common-subset safety correction and local campaign | removed the project-added all-RBC ACS completion shortcut, made BBA count only AUX values admitted by local BV-broadcast, retriggered pending AUX evaluation on admission, added bounded decision diagnostics and 1,000 fixed reordered-delivery schedules, and automated resumable local safety/liveness/identity gates | prevent asynchronous delivery order from producing different proposer subsets and block invalid latency evidence before EC2 | `sbc/hbbft`, `bloc-node/scripts/run-acs-safety-campaign.ps1`, canonical docs | repeated module tests and Linux race tests passed; persistent n4/batch-128 passed 100/100 measured slots; n4/n7 batches 8/32/128 passed 180/180 measured slots; bloc-node/BTE suites and focused Merge + Plan benchmark passed; no AWS resources allocated | run one clean EC2 n4/batch-128 recovery probe before rerunning optimized cross-AZ n4/n7 |

- 2026-07-14 | `M3` | optimized cross-AZ campaign and recovery automation | ran the optimized `n=4` cross-AZ phase, stopped before `n=7` after divergent ACS outputs, reproduced the failure after a clean cluster restart, hardened the kept-environment runner with CSV batch input, bounded health checks, cluster-wide restart ordering, aligned initial slots, UTF-8-without-BOM configs, strict result acceptance, and automatic operator logs, then destroyed and verified all AWS resources | prevent invalid correctness samples from becoming latency evidence and make future failure probes executable without model intervention | `deploy/ec2/rerun-a1-pilot-existing.ps1`, canonical docs, ignored campaign artifacts | batches 8/32 passed 30/30; batch 128 passed 7/30 before disagreement; clean probe reproduced 3-list/96-transaction versus 4-list/128-transaction ACS decisions; Terraform destroyed 20 resources and authenticated checks were empty | fix the ACS agreement divergence before rerunning optimized cross-AZ `n=4/n=7` |

- 2026-07-14 | `M3` | recurring EC2 campaign automation | added robust comma-separated matrix parameters, unattended approval handling, hard phase-level operator/batch bounds, and automatic matched campaign comparison tables/reports/charts for same-AZ and cross-AZ M3 runs | make frequent metric campaigns executable as one guarded command without model intervention and prevent malformed PowerShell arrays from allocating unintended resources | `deploy/ec2/run-a1-pilot.ps1`, `deploy/ec2/run-m3-*.ps1`, `latency-charts/*`, canonical docs | PowerShell parser checks passed; malformed node count 47 was rejected before AWS access; 17 Python tests passed | run the optimized cross-AZ campaign against the July 6 baseline |

- 2026-07-14 | `M3` | EC2 Merge/Plan attribution analysis | completed accepted same-AZ Compute Flex `n=4/n=7` phases, made the analyzer accept an independently invalid optional T3 comparison without admitting partial samples into headline statistics, and preserved the failed T3 phase in a diagnostic appendix | establish that isolated EC2 batch-128 ciphertext decoding is 195.1 ms p50 and 98.0% of Merge + Plan while reporting T3 temporal instability without cherry-picking | `latency-charts/*`, `docs/STATUS.md`, `docs/WORKFLOWS.md`, ignored campaign artifacts | 4 targeted analyzer tests passed; both accepted phases contain 30 successful consistent runs per batch, complete node metrics, healthy Prometheus snapshots, one image digest, and empty authenticated teardown checks; T3 retained 55/60 successful measured runs and failed acceptance after a timeout | profile and optimize ciphertext decoding locally before requesting budget for another optimized-image EC2 comparison |

- 2026-07-14 | `M3` | exact-image campaign resume | allow the Merge/Plan campaign to reuse a prebuilt local image and require its local image ID to match the resumed phase digest before allocating the next phase | preserve one-image attribution after runner-only commits and avoid spending Free-plan credits on an invalid comparison | `deploy/ec2/run-merge-plan-attribution.ps1` | PowerShell parser check and local Docker image inspection | resume n7 phases from the accepted `sha256:118f...` image |

- 2026-07-14 | `M3` | blocked measurement identity and campaign resume | namespace merged evaluator run IDs by measurement block, exclude warmups from EC2 acceptance and analysis, and allow already-completed/cleaned phases to be validated and resumed without provisioning them again | preserve all 30 measurements per batch while avoiding repeated AWS allocation after an artifact-only validation failure | `deploy/ec2/run-a1-pilot.ps1`, `deploy/ec2/run-merge-plan-attribution.ps1`, `latency-charts/*` | PowerShell parser checks and latency-chart tests | resume the completed Compute Flex n=4 evidence, then run Compute Flex n=7 and T3 n=7 |

- 2026-07-14 | `M3` | EC2 campaign automation, evaluator CSV, latency analysis | added a same-AZ Merge + Plan attribution campaign over Free-plan-eligible Compute Flex n=4/n=7 and T3 burstable n=7 operators, balanced three-block measurement ordering and drift summaries, one-image digest enforcement, EC2 host metadata, selected-ciphertext CSV evidence, strict campaign validation, comparison tables, Markdown reporting, and PNG/SVG charts | determine how much EC2 Merge + Plan latency is ciphertext decoding and separate node-count effects from instance-class and local-host effects without claiming fixed CPU performance or p99 | `deploy/ec2/*`, `bloc-node/internal/app/eval_suite.go`, `latency-charts/*`, canonical docs | PowerShell parser checks passed; source guard blocked relevant uncommitted work before AWS access; bloc-node and BTE Go suites passed; 13 latency-chart tests passed including synthetic three-phase analysis; Terraform initialized and validated | run the three EC2 phases sequentially and present the generated report |

- 2026-07-13 | `M3` | `bloc-node`, BTE, latency charts | split merge/plan timing into five additive substages, added deterministic disjoint/overlap campaign benchmarks and reports, eliminated duplicate inclusion-list hashing and merge fee parsing, added exact-placeholder duplicate fast-path validation, and planned directly from decoded canonical ciphertext encodings without reserializing batch IDs | make the second-largest protocol stage attributable and remove demonstrable duplicate work without changing protocol identities | `bloc-node/internal/app/*`, `bloc-node/scripts/run-merge-plan-campaign.ps1`, `bte/btd-impl-main/be/*`, `latency-charts/*`, canonical docs | matched local baseline/optimized campaigns with 10 benchmark samples and 60/60 successful consistent evaluator runs per phase; batch-32/128 pipeline benchmark medians improved 6.1%-17.5%; evaluator merge/plan medians improved 11.0%-14.3%; charts generated; Go and Python suites passed; bounded decoder fuzz passed 117,109 executions; race validation was attempted but requires a CGO `gcc` toolchain unavailable on this Windows host | rerun the targeted race test in Linux/WSL, rebuild the EC2 image, and keep future optimized-image evidence separate from earlier M3 samples |

- 2026-07-06 | `M3` | EC2 campaign automation, Terraform | added generated multi-AZ subnet support to the EC2 Terraform module, added a Windows M3 cross-AZ synthetic wrapper, and ran the `n=4/n=7` cross-AZ campaign cleanly | collect the first same-region cross-AZ comparison dataset while keeping the same cost controls and cleanup behavior as the same-AZ path | `deploy/ec2/terraform/main.tf`, `deploy/ec2/terraform/variables.tf`, `deploy/ec2/run-a1-pilot.ps1`, `deploy/ec2/run-m3-cross-az.ps1`, `docs/STATUS.md`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md`, `results/ec2/m3-cross-az-synthetic-20260706t122922z/`, `results/charts/m3-cross-az-synthetic-20260706t122922z/` | PowerShell parser checks passed for EC2 runner scripts; `terraform fmt -check -diff` and `terraform validate` passed; Docker and AWS STS preflights passed; M3 cross-AZ ran `n=4` and `n=7` with batches 8/32/128, 5 warmups, and 30 measured repetitions per batch; all 180 measured runs succeeded and were cross-node consistent; Prometheus saw 4/4 and 7/7 targets up; charts generated; Terraform destroy completed for both phases; cleanup verification and AWS follow-up checks found no tagged instances, volumes, VPC, ECR repository, temporary key pair, IAM role, or instance profile | compare same-AZ and cross-AZ chart outputs, then decide whether to request a vCPU quota increase for comparable `n=10` EC2 evidence |
- 2026-07-05 | `M3` | EC2 campaign automation | added the Windows M3 same-AZ synthetic campaign wrapper, resource sampling in EC2 phases, EBS volume tags, and expanded cleanup verification for volumes and IAM leftovers | promote the clean A1 pilot into a thesis-grade 4/7/10-node same-AZ synthetic metric path without weakening AWS cost guardrails | `deploy/ec2/run-m3-same-az.ps1`, `deploy/ec2/run-a1-pilot.ps1`, `deploy/ec2/terraform/main.tf`, `docs/STATUS.md`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md` | PowerShell parser checks passed for EC2 runner scripts; `git diff --check` passed with only existing CRLF warnings; `terraform fmt -check -diff` and `terraform validate` passed; `cd bloc-node && go test ./...` passed; `latency-charts\.venv\Scripts\python.exe -m pytest latency-charts` passed | run the M3 wrapper starting with the 4-node phase, inspect results, then continue to 7 and 10 nodes |
- 2026-07-05 | `M3` | docs, roadmap | removed inactive orchestrated-container guidance from the active roadmap, status, validation, workflow, architecture, and module README narrative while retaining a single out-of-scope artifact note | keep thesis metric collection focused on VM/EC2-per-sidecar campaigns and avoid presenting the old deployment rehearsal as an active deployment or validation path | `docs/ROADMAP.md`, `docs/STATUS.md`, `docs/VALIDATION.md`, `docs/WORKFLOWS.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md`, `bloc-node/README.md` | documentation consistency scan with `rg` plus targeted `git diff --check` | revive the retained manifests only if a future milestone explicitly brings that deployment path back into scope |
- 2026-07-05 | `M3` | EC2 pilot runner, charts | changed A1 pilot network characterization from ICMP ping to controller-to-operator HTTP `/healthz` timing, ran the automated A1 same-AZ pilot cleanly, and made the chart loader accept UTF-8 BOM manifests emitted from Windows artifacts | default EC2 security groups do not allow ICMP, while BLOC traffic uses HTTP/libp2p ports; ping loss was misleading pilot evidence; generated PowerShell JSON artifacts need to remain chart-compatible | `deploy/ec2/run-a1-pilot.ps1`, `latency-charts/src/bloc_latency_charts/data.py`, `latency-charts/tests/test_charts.py`, `docs/STATUS.md`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md`, `results/ec2/bloc-ec2-a1-pilot-same-az-n4-20260705-192544/`, `results/charts/bloc-ec2-a1-pilot-same-az-n4-20260705-192544/` | PowerShell parser validation passed for both EC2 runner scripts; targeted `git diff --check` passed with only existing CRLF warnings; Docker, AWS STS, and Terraform validation preflights passed; A1 pilot ran 4 `t3.small` operators plus 1 `t3.small` controller for batches 8/32/128 with 1 warmup and 3 measured repetitions per batch; all measured runs succeeded and were cross-node consistent; Prometheus saw 4/4 targets up; HTTP timing succeeded 5/5 to each operator before and after; Terraform destroy completed and AWS follow-up checks found no tagged instances, volumes, VPC, ECR repository, temporary key pair, IAM role, or instance profile; `latency-charts\.venv\Scripts\python.exe -m pytest latency-charts` passed; charts generated from the pilot CSV output | promote the runner shape to the next controlled M3 metric increment |
- 2026-07-05 | `M3` | EC2 pilot runner | added keep-alive debugging support to the Windows A1 runner plus a rerun script that reuses a kept EC2 environment to push a fresh image, restart operator containers, and run scenarios with explicit slot offsets | avoid slow and costly Terraform create/destroy cycles while iterating on runner, image, or evaluator bugs | `deploy/ec2/run-a1-pilot.ps1`, `deploy/ec2/rerun-a1-pilot-existing.ps1`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md` | PowerShell parser validation passed for both scripts; targeted `git diff --check` passed | launch the next pilot with `-KeepResourcesOnFailure`, use `rerun-a1-pilot-existing.ps1` for image/evaluator iteration, and destroy the kept Terraform workdir promptly after a clean run |
- 2026-07-05 | `M3` | EC2 pilot runner | added a Windows PowerShell A1 pilot runner and made it the current supported path for the same-AZ 4-operator pilot while keeping the bash runner as an optional future Linux/WSL controller path | continue EC2 pilot automation from the working Windows Codex environment instead of blocking on WSL distro/tooling issues | `deploy/ec2/run-a1-pilot.ps1`, `docs/STATUS.md`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md` | PowerShell parser validation passed; `git diff --check` found no whitespace errors beyond existing CRLF warnings; `terraform fmt -check -diff deploy/ec2/terraform` passed; `terraform -chdir=deploy/ec2/terraform init -input=false` and `terraform -chdir=deploy/ec2/terraform validate` passed | run the Windows runner once local Docker, AWS CLI, and Terraform preflights pass, then inspect `results/ec2/<experiment-id>/` artifacts and cleanup verification |
- 2026-07-05 | `M3` | EC2 pilot runner | aligned ECR-backed experiment ids with the scoped `bloc-ec2-*` IAM policy, isolated each runner Terraform workdir under the result artifact directory, added an `ecr`/`ssh-load` image-distribution switch, and moved a bounded Docker server check plus image build ahead of EC2 key creation and Terraform apply | prevent IAM-prefix drift, stale Terraform variables, and local Docker failures from creating cloud resources before the pilot can run | `deploy/ec2/run-a1-pilot.sh`, `deploy/ec2/terraform/main.tf`, `deploy/ec2/terraform/variables.tf`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md` | `bash -n deploy/ec2/run-a1-pilot.sh`; runner help check; clean Terraform validation from an isolated copy; AWS cleanup verification after interrupted pilot destroy found no EC2 instances, VPC, ECR repository, IAM role, or instance profile, and the stale EC2 key pair was deleted; Docker remains unavailable from this Codex shell because the socket still returns permission denied | fix Docker Desktop or WSL Docker socket access for the shell running the pilot, confirm `timeout 20 docker version --format '{{.Server.Version}}'` works, then rerun the A1 pilot |
- 2026-07-05 | `M3` | EC2 deployment automation | added a Linux-first A1 pilot runner that provisions the 4-operator same-AZ EC2 campaign, pushes the sidecar image to ECR, deploys host-local Compose services, runs batch-size 8/32/128 `eval-remote` pilot scenarios, collects network/Prometheus/log/evaluator artifacts, merges scenario CSV/JSON outputs, optionally generates charts, and destroys resources in a cleanup trap | turn the manual EC2 smoke into a repeatable cost-controlled pilot before thesis-scale runs | `deploy/ec2/run-a1-pilot.sh`, `docs/WORKFLOWS.md`, `docs/VALIDATION.md`, `docs/STATUS.md` | static review only in this Windows environment; WSL has no Linux distro installed, so bash execution was not available here | run the script from WSL Ubuntu or a Linux controller with AWS CLI, Terraform, Docker, `jq`, SSH, and SCP installed, then inspect the A1 pilot artifacts |
- 2026-07-05 | `M2`/`M3` | EC2 deployment, validation, docs | ran the first cost-controlled EC2-per-sidecar smoke with 4 `t3.micro` operator instances, 1 `t3.small` controller, ECR image distribution, host-local operator Compose, controller Prometheus/Grafana, and `eval-remote` from the controller | prove the VM/EC2-per-sidecar deployment path can collect protocol metrics on independent instances before longer campaigns | `deploy/ec2/*`, `docs/STATUS.md`, `.gitignore` | `terraform fmt -check -diff`; `terraform init`; `terraform validate`; `cd bloc-node && go test ./...`; Docker build/push to ECR; Terraform apply of 16 expected low-cost resources; `/healthz` and `/metrics` passed on all 4 operators; Prometheus saw 4/4 targets up; `eval-remote --batch-size 8 --warmups 0 --repetitions 1` succeeded with all node outputs consistent and `total_slot_us=90166`; artifacts collected under ignored `deploy/ec2/artifacts/ec2-smoke-20260705-1149/`; Terraform destroy removed all 16 resources and follow-up AWS checks found no running/stopped smoke instances, no smoke VPC, empty Terraform state, and no ECR repo | automate the copy/start/check sequence, then run repeated M3 EC2 campaigns with the same explicit destroy guardrail |
- 2026-07-04 | `M2`/`M3` | `bloc-node`, deployment, docs | added an EC2 inventory-to-config command plus host-local operator Compose, controller Prometheus/Grafana Compose, Terraform inventory skeleton with ECR repository and EC2 ECR-readonly instance profile support, and EC2 workflow docs | make the one-EC2-per-sidecar thesis deployment path reproducible without multi-host Compose or copied AWS credentials on operator hosts | `bloc-node/internal/app/ec2_config.go`, `deploy/ec2/*`, canonical docs | `cd bloc-node && go test ./...`; `terraform fmt -check -diff`; `terraform init`; `terraform validate`; first `terraform plan`; first apply blocked before EC2 creation by missing `ecr:CreateRepository` and `iam:CreateRole`, then partial networking resources destroyed successfully | add required ECR/IAM permissions to the deploy IAM user/group, then rerun plan/apply and collect the first `eval-remote` smoke artifacts |
- 2026-07-04 | `M2`/`M3` | docs, evaluation strategy | realigned the thesis evaluation strategy so local `eval-local`/`eval-suite` runs remain the clean protocol baseline, Docker Compose is a local deployment rehearsal, and VM/EC2-per-sidecar is the primary distributed metric-gathering environment | avoid treating one managed orchestrated-container cluster as the main DVT-style operator-independence model and reduce orchestration-specific measurement confounders | `docs/STATUS.md`, `docs/VALIDATION.md`, `docs/WORKFLOWS.md`, `docs/ROADMAP.md`, `docs/DECISIONS.md`, `docs/ARCHITECTURE.md` | documentation-only change; canonical-doc consistency review | plan the VM/EC2-per-operator deployment recipe |
- 2026-07-04 | `M2` | deployment, docs | fixed the earlier local orchestrated-container sidecar rehearsal by making pods start in parallel, adding startup-probe protection before liveness, publishing not-ready service DNS records for peer bootstrap, ignoring generated `bloc-node` cluster configs, and adding a local port-forward remote-evaluator config | allow all static libp2p peers to exist and resolve each other before readiness, while keeping trusted-dealer local config out of git | earlier deployment manifests, `.gitignore`, earlier local remote-evaluator config, `docs/STATUS.md` | `cd mempool-il && go test ./...`; `cd bloc-node && go test ./...`; Docker image build; local orchestrated-container rollout 4/4 ready; `/healthz` and `/metrics` over port-forward; `eval-remote` 1/1 success in the earlier local rehearsal result directory | primary distributed metrics move to VM/EC2-per-sidecar |
- 2026-06-30 | `M2`/`M3` | docs, workflow | clarified that local tests, local evaluator runs, and Compose runs are preflight checks only, while distributed sidecar runs are the current evidence target | keep future development focused on deployability and distributed thesis evidence instead of local timing numbers or chart generation as ends in themselves | `docs/STATUS.md`, `docs/VALIDATION.md`, `docs/WORKFLOWS.md` | documentation-only change; canonical-doc consistency review | superseded by the 2026-07-04 VM/EC2-per-sidecar evaluation strategy |
- 2026-06-28 | `M3` | `mempool-il`, `bloc-node`, deployment, docs | added corpus-backed `replay-placeholder` source mode that validates real signed Ethereum target transactions, encrypts them once into BLOC encrypted payloads, exposes mock placeholder metadata, and lets `eval-remote`/`eval-suite` select `--tx-source mock-placeholder` without changing synthetic defaults | make distributed latency evidence use realistic target transaction payloads without pretending public mempool transactions are native BLOC placeholders | `mempool-il/internal/mempool/replay_placeholder.go`, `mempool-il/internal/inclusion/builder.go`, `bloc-node/internal/app/provider.go`, evaluator tx-source options, Docker Compose mock-placeholder artifacts, canonical docs | `cd mempool-il && go test ./...`; `cd bloc-node && go test ./...`; Docker Compose mock-placeholder stack; `eval-remote` 2/2 measured successes from slots 2-3; chart generation | expand the corpus beyond the committed fixture before thesis-scale distributed runs |
- 2026-06-28 | `M3` | `mempool-il`, docs | changed mock-placeholder normalization so `encrypted_payload_hex` is parsed from signed placeholder transaction calldata instead of being treated as a parallel generated field; placeholder requested gas now drives BLOC selected-gas accounting while carrier gas remains metadata | align the mock mempool with the real intended flow where public mempool objects are placeholder transactions carrying encrypted payloads in calldata | `mempool-il/internal/mempool/classifier.go`, `mempool-il/internal/mempool/replay_placeholder.go`, docs | `cd mempool-il && go test ./...`; `cd bloc-node && go test ./...`; full mock-placeholder Compose recreate; `eval-remote` 1/1 measured success in `results/distributed/compose-mock-placeholder-calldata-clean` | expand the corpus beyond the committed fixture before thesis-scale distributed runs |
- 2026-06-28 | `M2` | `bloc-node`, deployment, Grafana, docs | replaced the hand-rendered `/metrics` text with official Prometheus collectors, seconds-based slot/HTTP latency histograms, `_total` counters, bounded labels, normalized HTTP instrumentation, and histogram-safe Grafana queries | make distributed sidecar visibility Prometheus/Grafana-native instead of a scrapeable copy of local evaluator artifacts | `bloc-node/internal/app/metrics.go`, node lifecycle metrics, Grafana dashboard JSON, canonical validation/decision docs | `cd bloc-node && go test ./...` | build/run Compose, confirm Prometheus targets and Grafana PromQL panels, then run `eval-remote` smoke |
- 2026-06-28 | `M2` | `bloc-node`, deployment, docs | added deployment-ready sidecar support with listen/advertise config fields, `NODE_ID` startup, Prometheus-compatible `/metrics`, structured slot logs, `eval-remote`, Docker image, Compose Prometheus/Grafana smoke stack, earlier orchestrated-container manifests, and deployment-first roadmap/status/validation docs | make distributed sidecar latency/performance evidence collectable before Builder API compatibility work | `bloc-node/internal/app/*`, `bloc-node/Dockerfile`, `deploy/*`, canonical docs | `cd bloc-node && go test ./...` | build/run Compose smoke, run `eval-remote`, then collect distributed sidecar charts |
- 2026-06-25 | `M1` | `sbc/hbbft`, `bloc-node`, evaluator | fixed remaining 7/10-node local liveness stalls by serializing node-local ACS driver steps, adding ACS/RBC/BBA progress snapshots, correcting BBA dual-BVAL accounting and later-epoch self-BVAL handling, adding an all-RBC ACS completion path, and making evaluator result polling report true missing node IDs instead of ready prefixes | make the local controlled M1 campaign deterministic enough to produce professor-facing baseline evidence without masking failed runs | HBBFT ACS/BBA, `bloc-node` node status/evaluator polling, focused tests | `bloc-node go test ./...`; `sbc/hbbft go test ./...`; 27/27 reduced 4/7/10 matrix; 30/30 targeted 7/10 stress matrix | rerun the complete 315-sample `m1-baseline` and regenerate charts |
- 2026-06-25 | `M1`/`M2` | docs, BTE | documented that integrated `bloc-node` and M1 runs already use BEAT-MEV `Opt-2` deterministic sub-batching, while normal/`sqrt(B)`/parallel/512-batch comparisons remain M2 benchmark dimensions | make experiment interpretation discoverable from Markdown without inspecting BTE code | `docs/ARCHITECTURE.md`, `docs/VALIDATION.md`, `docs/WORKFLOWS.md`, `docs/STATUS.md`, `bte/btd-impl-main/README.md`, `bte/btd-impl-main/CLUSTER_BTE.md`, `bte/btd-impl-main/TESTING.md` | documentation-only change; link/ownership review | decide whether to add explicit evaluator flags for BTE optimization variants during M2
- 2026-06-24 | `M1` | `bloc-node`, `sbc/hbbft`, evaluator | added replaceable slot-scoped node state, strict slot-aware control calls, stale-envelope rejection, cascading idempotent ACS shutdown, persistent operator-count clusters, deterministic grouped scheduling, recovery restarts, and separate cluster/preparation/submission evidence | amortize local setup without contaminating latency samples or weakening failure evidence | node lifecycle, HBBFT slot teardown, persistent evaluator, tests, canonical architecture/workflow/validation docs | existing app tests passed; 4-run persistent 4-node smoke and 6-run 4/7/10 matrix both passed, including batch 128 and one combine attempt on every node | run the complete persistent 315-sample M1 campaign and regenerate charts
- 2026-06-24 | `M1` | `sbc/hbbft`, `bloc-node`, evaluator, charts | made message-queue drains atomic, prevented ACS completion before all positively decided RBC outputs exist, serialized node-local BTE combination, removed raw TCP/gob transport, standardized protobuf envelopes over direct libp2p streams, added deterministic retrying peer connections and complete stream writes, reused evaluator HTTP connections, and reduced M1 to 9 libp2p scenarios/315 runs | eliminate message loss, premature nil ACS outputs, BTE races, socket churn, and duplicate transport evidence | HBBFT consensus state, `bloc-node/internal/app/*`, evaluator/profile tests, chart tests, scripts, canonical architecture/decision/validation docs | `bloc-node go test ./...`, all BTE tests, 9 chart tests, 10/10 readiness runs, and 18/18 reduced matrix runs passed; the full 315-run campaign completed with 298 successes and 17 result timeouts | diagnose remaining 7/10-node liveness stalls, add persistent-cluster campaign execution, then collect a valid M1 dataset and figures
- 2026-06-21 | `M1` | `bloc-node` evaluator and `latency-charts` | added the 18-scenario `m1-baseline` profile, resolved campaign metadata, fixed configuration fields in run CSVs, scalable readiness timeout, and separate `results/charts/<experiment-id>` output discovery | make the complete local baseline a single reproducible command while keeping figures separate from raw results | evaluator, chart CLI/tests, module READMEs, canonical validation/status docs | Go unit tests, 8 chart tests, reduced profile integration run, and chart output-path smoke passed | execute the full 630-run profile and generate `results/charts/m1-baseline`
- 2026-06-21 | `M1` | `latency-charts` | added a Python CLI that converts evaluator run measurements into SVG/PNG p50/p95 scaling, mean critical-path breakdown, and raw-run distribution charts | make configuration-dependent latency results directly inspectable and suitable for later thesis figures | `latency-charts/*`, root navigation, `docs/VALIDATION.md`, `docs/STATUS.md` | 5 pytest cases passed; all six figures rendered and visually inspected against the reduced TCP/libp2p dataset | generate figures from the full baseline
- 2026-06-21 | `M1` | `bloc-node` evaluator | added monotonic microsecond stage timing, critical-node aggregation, seeded repeated configuration suites, configuration manifests, raw/summary CSV and JSON artifacts, stricter cross-node consistency checks, and a libp2p full-mesh readiness barrier | collect configuration-linked local latency statistics without overstating them as DVT deployment evidence | `bloc-node/internal/app/*`, `bloc-node/README.md`, `docs/VALIDATION.md`, `docs/STATUS.md` | `go test ./internal/app`; reduced 4-node batch-8 TCP/libp2p suite with 1 warmup and 2 measured repetitions passed all 6 runs | run the full 12-scenario baseline
- 2026-06-21 | `M0` | docs/repo | created canonical root documentation spine and task-routing `AGENTS.md` | reduce fragmented documentation and make Codex navigation predictable | `README.md`, `AGENTS.md`, `docs/*`, module READMEs | documentation link review | keep consolidating redundant module-local notes over time
- 2026-06-21 | `M0` | docs/repo | added live `STATUS.md` and rebuilt the roadmap around RQ-tagged phased milestones | make project state resumable and keep execution linear | `docs/STATUS.md`, `docs/ROADMAP.md`, routing docs | documentation consistency review | use `M1` to drive the next implementation and evaluation cycle
